# 战斗开始后的行为逻辑 —— 详细落地方案

> 状态：方案定稿（2026-09-06 晚，真机 run2/3/4 诊断后）
> 范围：只解决「战斗开始后的行为逻辑」；先出方案与离线测试，暂不再真机自动点击
> 关联：`PROGRESS.md`（第6轮待修③）、`decision/battle_fsm.py`、`decision/coach.py`、`tools/coach_session.py`、`tools/coach_masks.py`、`decision/deploy_planner.py`、`decision/resource_policy.py`

---

## 1. 背景与目标

用户确认的「打资源」正确流程是：

> **结束按钮常驻 ≠ 可点** → ① 先算可下兵位置（排除红区）→ ② 识别所有敌方资源建筑 → ③ 资源算法判定「能否获取」→ ④ 下兵 → ⑤ 所有可获取资源建筑都打光 → 才点结束 → ⑥ 回村进入下一轮循环。

本次只改**战斗开始后（进入敌方村庄后）**这一段。村庄→搜索→开战的链路不在本次范围（另见 §9）。

---

## 2. 现状诊断（证据来自 2026-09-06 真机会话 run2/3/4 的录制帧）

| # | 缺陷 | 证据 | 根因（代码） |
|---|---|---|---|
| 1 | **过早结束战斗** | run3 step1：战斗剩 1:18、敌方资源可见，教练判「战斗结束」并点「结束战斗/返回」；用户真机当场复现 | CoC 的红色「结束战斗」按钮**整场常驻**；而 `battle_fsm.py:131-143` 把 `end_battle_button` 判断放在 `policy(signals)` 之前 → 看到按钮就结束；教练提示词 `coach.py:160-162` 也把「看到结束/返回按钮」当结束信号 |
| 2 | **资源列表缺失** | 本地 fsm_v4 真机 0 检出，`signals.enemy_resources` 恒空 → 全程只靠教练逐帧给**单个** deploy 目标 | 教练识别的资源建筑（`missed_objects` / 单目标 deploy）**没有系统回填**进 `enemy_resources`，`resource_policy` 吃不到「所有资源建筑」，无法做「算法判定可获取/已达阈值轮换」 |
| 3 | **坐标漂移 / 画面误读** | run4：同一「进攻按钮」坐标在 x=242 ↔ x=2332 间横跳；把军队/模式面板误读为「村庄」 | 教练提示词对真实 UI 与坐标判据的描述和实际不符（偏误主要在开战前；战斗内 deploy 坐标相对稳定） |

> 已修复并保留（不在本次改动内）：`executor/validator.py` 不再对 `tap` 校验 `troop_coords` —— 教练非 deploy 动作填默认 `[0,0]` 导致所有 tap 被拦成 wait 的 bug 已修，150 测试 + 审计通过。

---

## 3. 已确认决策

- 资源识别来源：**完整实现** —— 教练在战斗帧**枚举全部可见资源建筑**回填算法，而非只给单个目标。
- 结束判定：**连续 N 帧无可获取目标**才点结束（N 默认 3，可配），抗单帧漏检/画面抖动。
- 判定权威：结束/继续下兵由**确定性资源算法**决定；教练不再拥有「点结束」的决定权，只负责**提供视野内的资源建筑**与**兵种栏 troop_coords**、并对状态做 sanity。

---

## 4. 落地方案（分层）

### 4.1 复用的现成能力（不新造轮子）

| 能力 | 现成实现 | 用途 |
|---|---|---|
| 可下兵掩码（排除红区闭合面积+底部UI+边缘） | `tools/coach_masks.no_land_mask(screenshot)` | ① 算「哪些位置可下兵」 |
| 落点吸附到可下兵草地 | `tools/coach_masks.plan_deploy_point` / `snap_deploy_point` | ④ 落点不进红区、贴建筑 |
| 资源打分排序（价值/可达/安全/距离，跳过己方已满） | `decision/resource_policy.ResourcePolicy.scored(signals)` | ③ 算法判定「能否获取」+排序 |
| 目标轮换 + 重复上限 + 落点规划整合 | `decision/deploy_planner.DeployPlanner.plan` | ④ 最终选哪个目标/落点 |

### 4.2 `decision/battle_fsm.py` —— BATTLE 分支顺序修正

现状（约 `battle_fsm.py:131-143`）：

```python
if self.state is BattleState.BATTLE:
    if signals.end_battle_button is not None:          # ← 先判结束，错误
        return ... tap 结束战斗
    targets = self.policy(signals)
    ...
```

改为（**先算法后结束**）：

```python
if self.state is BattleState.BATTLE:
    targets = self.policy(signals)                     # ① 资源算法判可获取目标
    if targets:
        guard.on_targets()                             # 清空判空计数
        name, coords = targets[0]
        return BattleAction("deploy", name, coords)
    if signals.return_button is not None:              # 被动结算界面（输了/超时/已结束）
        return transit BATTLE_OVER, wait               # 该分支表示画面已是结算/结束界面
    if signals.end_battle_button is not None and guard.on_empty():
        return transit BATTLE_OVER, tap 结束战斗       # 连续 N 帧判空才点
    return wait
```

**新增极小组件 `EmptyTargetGuard`**（放 `decision/battle_fsm.py`，供 BattleFSM 与 coach_session 共用，避免判空逻辑两处分叉）：

```python
@dataclass
class EmptyTargetGuard:
    threshold: int = 3        # 连续判空多少帧才算「可获取资源全没了」
    streak: int = 0
    def on_targets(self) -> None: self.streak = 0
    def on_empty(self) -> bool:
        self.streak += 1
        return self.streak >= self.threshold
```

`BattleFSM.__init__(..., empty_frames_threshold=3)` 内部持有该 guard。
顺带修正 `BattleSignals.enemy_resources` 语义注释：列表中每个 (建筑名, 中心点) 均视为「算法会打分的候选」。

### 4.3 `decision/coach.py` —— 教练输出资源列表 + 重写战斗规则

**CoachVerdict 新增字段**：

```python
resource_buildings: list[tuple[str, tuple[int, int]]] = field(default_factory=list)
# 教练本帧看到的「全部可见敌方资源建筑 (6 类名, 归一化→像素中心)」
```

**build_coach_prompt 输出 JSON schema 新增**：

```jsonc
"resource_buildings": [
  {"type": "金矿", "center": [0.31, 0.45]},   // 归一化
  {"type": "圣水瓶", "center": [0.62, 0.30]}
],
```

**重写「战斗中」规则文案**（现在 `coach.py:160-162`），要点：
- 红色「结束战斗」按钮**整场常驻**；绝不因「看到结束按钮 / 有倒计时 / 某一帧视野暂无资源」就点结束。
- 只要视野内还有敌方资源建筑（6 类任一）→ 唯一正确动作是 deploy 到其中**可获取**的一个；**不要**点结束/返回。
- 只有**视野内不再有任何敌方资源建筑（判定打光/消失）**时，才允许点结束。
- 每一帧都要在 `resource_buildings` 里列出**全部**可见资源建筑的中心（不只单个目标），供算法打分排序。
- 复述坐标三铁律：归一化、6 类枚举名、别发明建筑名。

**_parse_verdict 解析 `resource_buildings`**：
- center 若在 [0,1] → 归一化乘 `image_size` 转像素；否则按 legacy viewed→orig 缩放；
- **过滤**：type 不在 `decision.resource_policy.RESOURCE_NAMES` 6 类、坐标越界/缺失 → 丢弃；
- 写入 `verdict.resource_buildings`。

### 4.4 `tools/coach_session.py` —— 资源回填 + 过早结束护栏

在 coach review 之后、`deploy_planner.plan` 之前插入两个逻辑：

**(a) 资源回填**：`verdict.resource_buildings` 合并进 `signals.enemy_resources`（按 `(name, coords)` 去重）。这样 `deploy_planner.plan(...)` 打分时真吃到「所有资源建筑」。

**(b) 过早结束护栏（模块级纯函数，便于单测）**：

```python
def _battle_end_guard(
    signals,                 # 已回填的 signals
    deploy_planner,
    final_action,            # 当前拟执行动作（dict）
    guard: EmptyTargetGuard,
    screenshot, detections,  # 供 deploy_planner.plan 的上下文
) -> dict:                   # 返回可能被改写的 final_action
    kind, target = final_action["kind"], final_action.get("target")
    if kind == "deploy":
        guard.on_targets()
        return final_action
    if kind == "tap" and target in ("结束战斗", "返回按钮"):
        plan = deploy_planner.plan(signals, screenshot=screenshot, detections=detections)
        if plan is not None:              # 仍有可获取目标 → 强制继续下兵
            guard.on_targets()
            return {kind:"deploy", target:plan.target_name,
                    coords:list(plan.landing_point),
                    troop_coords:final_action.get("troop_coords"), **meta}
        if not guard.on_empty():          # 连续判空未达阈值 → 暂不结束
            return {"kind":"wait", "target":"判空未达阈值，暂不结束战斗", ...}
        # 达阈值 → 放行原 tap 结束
    return final_action
```

关键点：
- 该 guard 让 **`deploy_planner.plan` 变成结束判定的唯一权威**：有可获取目标 → deploy；连续 N 帧 None → 才放行结束。
- 进入结束（`local_state == "战斗结束"`）时重置 guard 与 `deploy_planner.reset()`（现在只有 `战斗结束` 才 reset，保持）。

**新增 argparse**：`--end-empty-frames`（int，默认 3）→ 透传给 `EmptyTargetGuard` 与 `BattleFSM`。

---

## 5. 涉及文件清单

| 文件 | 改动 |
|---|---|
| `decision/battle_fsm.py` | BATTLE 顺序修正；新增 `EmptyTargetGuard`；`BattleFSM.__init__` 加阈值参数 |
| `decision/coach.py` | `CoachVerdict.resource_buildings`；prompt schema + 战斗规则重写；`_parse_verdict` 解析/过滤 |
| `tools/coach_session.py` | 回填 + `_battle_end_guard` + `--end-empty-frames` |
| `tests/test_battle_fsm.py` | 改结束用例 + 新增守卫/顺序用例 |
| `tests/`（新增 coach/coach_session 用例） | `resource_buildings` 解析；护栏 dry-run |
| `PROGRESS.md`（可选，落地后） | 记录本次交付与下次验收口径 |

---

## 6. 测试计划

**改现有（`tests/test_battle_fsm.py`）**
- `test_battle_end_button_finishes`：现在单帧即结束 → 改为「无资源时首帧 wait；连续 3 帧 end_battle_button 可见才 tap 结束」。

**新增**
- `test_battle_fsm.py`：
  - 有敌方资源 + end_battle_button 可见 → **deploy 不结束**；
  - deploy 后判空计数清零；
  - return_button 分支（被动结算）不受影响。
- coach 解析：`resource_buildings` 归一化→像素；非 6 类过滤；坐标越界丢弃。
- coach_session 护栏（dry-run 构造 verdict，无需真机）：
  - 教练拟 tap 结束、但回填后 signals 有可获取资源 → 强制转 deploy；
  - 教练拟 tap 结束、信号全空 → 前 2 帧 wait、第 3 帧才结束。

**离线回归（真机录制帧）**
- 用 `dataset/coach_sessions/20260906_035433`（run3，含战斗中+敌方资源帧）与 `20260906_035124`（run2）构造战斗帧输入，确认：不再点结束、走 deploy；打光后（构造空 signals）第 3 帧才点结束。

**质量门槛**
```bash
source activate multimodal-agent
python -m pytest tests -ra      # 全绿（现 150 + 新增）
python -m security.audit        # 0 发现
```

---

## 7. 真机验收口径（下次上机，跑通一次完整战斗）

1. 进入战斗后，日志出现多资源打分与 deploy，**没有**「刚开战就 tap 结束」；
2. 打光过程中即便某帧视野暂无资源，**连续 ≥3 帧**无目标才会 tap 结束；
3. 结束只发生在：连续判空 / 结算界面（return_button）；随后回村，进入下一轮（新一轮再次发起进攻）；
4. 每条 deploy 日志含 target_score / repeat / reason，落点 100% 在可下兵草地（不进红区/UI）。

---

## 8. 不做 / 后续（不在本次）

- 开战前链路：村庄「进攻按钮」入口与真实 UI（军队面板底部绿色「进攻」）不一致、教练坐标漂移（x 242↔2332）——单独跟踪，涉及 `decision/coach.py` 村庄/模式面板规则 + 也许需要「本地按钮检测能兜底」。
- 本地 fsm_v4 真机 0 检出 → 用 run2/3/4 已录帧做回流重训（检测器/状态分类器），把 LLM 触发率降下来。这是根治项，长期收益最大。
- OCR 顶栏判满（`farm-scope-reduction` 记忆：满不满用 OCR 顶栏），当前 `resources` 全 0，战斗内影响较小，先不动。

---

## 9. 风险与回滚

- **风险**：教练 `resource_buildings` 偶发漏报/误报 → 连续 3 帧判空后提前结束或过度下兵。缓解：阈值可配（`--end-empty-frames`）、`deploy_planner` 重复上限仍在、落点掩码强制吸附。
- **回滚**：本次只改 3 个源文件 + 测试，改动局部；任一环异常可 `git checkout` 对应文件回退（注意本项目根目录 `.git` 异常无 HEAD，建议改动前先 `cp` 备份 3 个文件）。
