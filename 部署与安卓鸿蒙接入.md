# 手机端接入与收尾部署

## 架构

当前收尾方案把模型和执行控制都放在电脑/服务器端，手机端只做控制台：

```text
Android/HarmonyOS 浏览器/PWA
  -> FastAPI 会话 API
    -> YOLO 识别器
    -> FSM 决策
    -> Qwen-VL 教练审查
    -> ADB 执行 / 视频 dry-run
    -> 回流训练
```

这样不需要先学 Android 应用开发。Android 和 HarmonyOS 浏览器都可以把控制台添加到主屏幕使用；后续要打成 APK/HAP 时，也只需要包一层 WebView。

## 视频部署目录

打资源视频放：

```text
dataset/video/resource/
```

捐兵视频放：

```text
dataset/video/donate/
```

支持 `mp4/mov/mkv/webm/avi`。也可以直接在手机控制台上传录屏，后端会自动保存到对应模块目录。

## 启动后端

在项目根目录执行：

```bash
source activate multimodal-agent

export VISION_MODEL_BASE_URL='https://dashscope.aliyuncs.com/compatible-mode/v1'
export VISION_MODEL_NAME='qwen3-vl-plus'
export VISION_MODEL_API_KEY="$(sed -n '2p' api.txt)"

uvicorn api.main:app --host 0.0.0.0 --port 8000
```

如果 8000 端口被占用，换一个端口即可，例如：

```bash
uvicorn api.main:app --host 0.0.0.0 --port 18000
```

手机和后端必须在同一局域网，或通过你能访问的内网/VPN 连接。控制台地址：

```text
http://<电脑局域网 IP>:8000/static/index.html
```

不要把这个服务直接暴露到公网；ADB 执行模式会真的操作游戏画面。

## 控制台用法

1. 填后端地址，页面会显示模型、教练、ADB 状态。
2. 选择模块：打资源或捐兵。
3. 选择运行方式：
   - 视频模拟：先跑 dry-run，不操作真机；
   - ADB 执行：连接模拟器或测试机后真实执行。
4. 点启动，页面轮询当前状态、FSM 状态、动作和 Qwen 教练纠正。
5. 需要中止时点停止；会话数据仍保存在 `dataset/coach_sessions/`。
6. 会话达到足够数量后，使用回流训练区域启动 YOLO + 状态分类器重训。

## API

核心接口：

| 方法 | 路径 | 用途 |
|---|---|---|
| GET | `/session/capabilities` | 检查模型/教练/ADB |
| GET | `/session/videos?module=farm|donation` | 列出视频 |
| POST | `/session/upload/video?module=farm|donation` | 上传录屏 |
| POST | `/session/start` | 启动 coach 会话 |
| GET | `/session/{session_id}` | 查询进度 |
| POST | `/session/{session_id}/stop` | 请求停止 |
| POST | `/session/retrain` | 回流重训 |
| GET | `/session/retrain/{job_id}` | 查询训练任务 |

## 批量视频标注与 v5 训练

把两类视频一次性交给标注器，保持不同视频间标签一致。命令在项目根目录执行：

```bash
export VISION_MODEL_BASE_URL='https://dashscope.aliyuncs.com/compatible-mode/v1'
export VISION_MODEL_NAME='qwen3-vl-plus'
export VISION_MODEL_API_KEY="$(sed -n '2p' api.txt)"

python -m tools.labeling.video_auto_label \
  --video dataset/video/resource/* dataset/video/donate/* \
  --out dataset/video_batch_v5 \
  --fps 0.25 --max-frames 30 --llm
```

这里每秒采样约 1/4 帧，单视频最多 30 帧；如果视频很短或想覆盖更多状态，可以把 `--max-frames` 调大，但 API 消耗也会增加。

标注完成后训练 v5 检测器和状态分类器：

```bash
python -m tools.train_yolo \
  --dataset dataset/video_batch_v5 \
  --base runs/detect/fsm_v4/weights/best.pt \
  --out runs/detect/fsm_v5 \
  --epochs 10

python -m decision.state_classifier \
  --train \
  --data dataset/video_batch_v5/state_labels.json \
  --out runs/state_classifier_v5
```

训练完成后先看 v5 在 `dataset/video_batch_v5/valid` 上的表现，再用控制台跑打资源和捐兵 dry-run；确认状态识别稳定后，才把环境变量切到 v5 权重并在 ADB 模式做小步真机验证。

`POST /session/start` 的最小示例：

```json
{
  "module": "donation",
  "source": "video",
  "video_path": "dataset/video/donate/0f5f314755781c0ad3f612b4c133c8fa.mp4",
  "max_steps": 10,
  "enable_coach": true
}
```

真实 ADB 模式改为：

```json
{
  "module": "farm",
  "source": "adb",
  "serial": "127.0.0.1:5555",
  "max_steps": 100,
  "enable_coach": true
}
```

## 使用新模型

当前 v4 模型：

```text
runs/detect/fsm_v4/weights/best.pt
runs/state_classifier_v4/best.pt
```

控制台重新发起回流训练时，默认输出 v5：

```text
runs/detect/fsm_v5/weights/best.pt
runs/state_classifier_v5/best.pt
```

训练完成并验收后，重启后端时指向新权重。也可以不显式设置变量，当前代码默认加载 v4：

```bash
export DETECTOR_MODEL='runs/detect/fsm_v5/weights/best.pt'
export STATE_CLASSIFIER_MODEL='runs/state_classifier_v5/best.pt'
uvicorn api.main:app --host 0.0.0.0 --port 8000
```

`/session/capabilities` 会显示当前生效的模型路径。

## v5 当前验收结果

首轮 v5 已用 11 个视频训练完成：`84` 张关键帧、`488` 个框、`68/16` 训练/验证划分。

```text
状态分类器 val accuracy: 0.8125
检测器 mAP50:           0.0842
检测器 mAP50-95:        0.0292
```

结论：状态分类器已有可用基线，但检测器仍在数据量不足区间；不要把 v5 切到 ADB 真机执行。继续保留 v4 作为默认权重，用 v5 只做 dry-run、对比评测和数据诊断。下一轮优先补充每个按钮/兵种/建筑类别至少 20 个高质量框，并保证捐兵流程的状态分布更均衡。

## 教练坐标协议

Qwen 教练现在统一输出归一化坐标 `[0.0, 1.0]`。后端在执行和回流训练前会按截图真实宽高换算；旧版教练输出按 `1280×960` 像素逻辑画布的坐标也会做兼容换算。回流数据生成时会映射到 17 个标准类别，并自动生成 `dataset.yaml`。

## ADB 准备

如果系统有包管理器，可以安装：

```bash
sudo apt install android-tools-adb
```

当前项目已经内置免 sudo 的官方 ADB，路径是：

```text
tools/android-platform-tools/platform-tools/adb
```

后端会自动优先使用它；如果该文件不存在，才会回退到系统 `PATH` 中的 `adb`。

网络 ADB 常用连接方式：

```bash
adb connect 192.168.31.111:5555
adb devices
```

USB 连接则开启开发者选项和 USB 调试后直接 `adb devices`。只有在控制台选择 ADB 执行时才会操作真机；视频模拟始终是 dry-run。

```bash
export DETECTOR_MODEL='runs/detect/fsm_v4/weights/best.pt'
export STATE_CLASSIFIER_MODEL='runs/state_classifier_v4/best.pt'
uvicorn api.main:app --host 0.0.0.0 --port 8000
```

`/session/capabilities` 会显示当前生效的模型路径。
