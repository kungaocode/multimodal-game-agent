# 状态转换识别测试计划（阶段 13 实验评估体系 · 打资源 / 捐兵）

> 2026-09-02 · 基于用户叙述（打资源完整流程 + 捐兵流程）设计的后续测试与训练计划。
> 配套工具：`tools/eval_state_transition.py`（状态转换识别测试，已实现）、
> `tools/labeling/llm_samples_to_yolo.py`（回流样本→YOLO，已实现）、
> `tools/labeling/ocr_buttons_to_yolo.py`（OCR 半自动按钮标注，已实现）、
> `decision/battle_fsm.py`（含搜索环节的完整打资源状态机，已实现）。

## 1. 目标

让「本地模型」成为有限状态机（FSM）的**识别器**：从一帧截图识别出状态转换标志，
驱动状态机按预期流转。**不需要模型本身多强**，只需可靠识别以下信号：

| 信号 | 类别 | 所属状态机 |
|------|------|-----------|
| 资源建筑（金矿/圣水收集器/储金罐/圣水瓶/暗黑重油罐/暗黑重油钻井） | 6 类 | 打资源 |
| 进攻按钮 | 1 类 | 打资源 |
| 搜索对手按钮 | 1 类 | 打资源 |
| 下一个按钮（放弃当前对手） | 1 类 | 打资源 |
| 结束战斗按钮 | 1 类 | 打资源 |
| 返回按钮（回营/战斗结束返回） | 1 类 | 打资源 |
| 增援按钮 | 1 类 | 捐兵 |
| 部落消息/请求列表（后续可加） | 1 类 | 捐兵 |

共 **12 类**（`perception/fsm_labels.py`）。

## 2. 打资源流程要点（用户叙述 → 状态机）

```
村庄待机 ── 检查自己资源（OCR 顶栏：金/圣水/黑油是否满）
  ├─ 全部已满 → 停止
  └─ 识别「进攻按钮」→ 点进攻 → 进入常规战搜索 ─→ 搜索中
搜索中   ── 识别对手村庄界面（战斗场景：敌方资源建筑 + 可获得战利品）
  ├─ 资源评估算法（decision/resource_policy.py）判「值不值得」
  │     ├─ 值得 → 点击放兵：将特定兵种放在资源建筑处 → 开始战斗 ─→ 战斗中
  │     └─ 不值得 → 点击「下一个」继续搜索对手（保持搜索中）
  └─ 识别「回营」→ 返回村庄待机
战斗中   ── 打掉目标建筑（罐子/采集器；不必全部，距离/防御因素可放弃）
  ├─ 识别「结束战斗」→ 战斗结束
  └─ 敌资源打光 / 兵力用尽 → 战斗结束 ─→ 战斗结束
战斗结束 ── 识别「返回」→ 返回村庄待机（循环检测）
```

## 3. 捐兵流程要点（用户叙述 → 后续 FSM）

```
捐兵模块（打开部落面板）
  打开左侧「消息列表」→ 搜索「请求援兵」条目
    有请求 → 点开 → 点击「增援」（绿色按钮）
      用户指定兵种 → 捐指定兵种
      未指定 → 自动选默认兵种（如气球兵）
    无请求 / 完成 → 关闭消息列表 → 返回主界面
```

捐兵 FSM 与捐兵信号类别（消息列表 / 请求条目 / 增援按钮 / 关闭按钮）在阶段 10 落地，
本测试计划先覆盖「识别出请求与增援按钮」的感知验收。

## 4. 数据现状与扩充计划

### 4.1 现状（final_test_dataset，9 张）
| 状态 | 图片 | 说明 |
|------|------|------|
| 村庄待机 | 1.jpg、969D0B80… | 有「进攻！」按钮 |
| 搜索中 | 8E51F4BD…（搜索对手/常规战）、A8B537A6…（搜索动画/回营） | |
| 战斗中 | 018FC1FE…、22F09569…（开战倒计时/可获得战利品/下一个/结束战斗） | |
| 捐兵-消息列表 | 1449A3F4…、C31DEB33…、EA356B88… | 「救命啊！我需要增援！」+ 增援按钮 |

**缺少的环节**：战斗进行中（下兵后）、战斗结束界面、战斗胜利/失败结算、
捐兵请求详情页、增援确认界面、增援完成反馈。

### 4.2 扩充方式（数据量小，用户将想办法扩大）
1. **手动截图**：按上面缺的环节补充，每环节 ≥ 20 帧（不同村庄/不同分辨率）。
2. **OCR 半自动标注**（`ocr_buttons_to_yolo.py`）：真实截图 + PaddleOCR 文本定位 →
   自动生成按钮类框（进攻/回营/下一个/结束战斗/搜索对手/增援），秒级扩充按钮标注。
3. **LLM 回流**（阶段 6 已落地）：`/perceive?mode=auto` 中本地置信度不足时调 qwen3-vl-plus，
   成功样本自动进 `dataset/llm_samples/` → `llm_samples_to_yolo.py` 转成标注。
4. **人工校正**：标注工具 `tools/labeling/app.py`（阶段 1 已建）人工复核按钮/资源框。

### 4.3 类别数据平衡
当前合成集（farm_synthetic 1200+260 张）只有 6 类资源建筑，**按钮类为 0**，
这是「模型无法识别状态转换标志」的直接原因。按钮类数据完全依赖 4.2 的真实数据扩充。

## 5. 训练计划（回流微调 + 轻量识别器）

```bash
# 1. 回流样本 → YOLO（教师蒸馏）
python -m tools.labeling.llm_samples_to_yolo --samples dataset/llm_samples --out dataset/llm_finetune
# 2. OCR 半自动按钮标注（真实截图 → 按钮框）
python -m tools.labeling.ocr_buttons_to_yolo --images final_test_dataset dataset/game_picture \
    --out dataset/fsm_real_buttons
# 3. 合并 6 类合成 + 回流 + OCR 按钮 → 12 类微调
python -m tools.labeling.finetune_fsm \
    --synthetic dataset/farm_synthetic \
    --llm dataset/llm_finetune \
    --buttons dataset/fsm_real_buttons \
    --base runs/detect/farm/weights/best.pt --epochs 20 --out runs/detect/fsm
```

- 基础权重：`yolov8n.pt` 或 farm 权重；轻量训练（<25 epochs，imgsz 512~640）。
- 验证集以真实截图为主（按钮/战斗界面），合成集作背景负样本。
- 微调完成后 `runs/detect/fsm/weights/best.pt` 即 FSM 识别器权重。

## 6. 状态转换识别测试（验收，工具已实现）

```bash
# 打资源 + 捐兵场景的状态识别：模型权重 + OCR 参考
python tools/eval_state_transition.py --images final_test_dataset --weights runs/detect/fsm/weights/best.pt --ocr

# 完整状态转换链（村庄→搜索→战斗→结束→返回）——按实际顺序的截图序列驱动 BattleFSM
python tools/eval_state_transition.py --images final_test_dataset \
    --sequence 1.jpg,A8B537A65DA5ED009F57C5B48618D349.jpg,018FC1FEC651489E0163B3D1862C3FB6.jpg \
    --weights runs/detect/fsm/weights/best.pt --json /tmp/seq_report.json
```

### 6.1 指标
1. **单帧状态识别准确率**：期望状态（labels.json） vs 信号规则判定状态；
   「捐兵」场景单独统计（属捐兵 FSM，不打资源 FSM 的准确率分母）。
2. **按钮信号检出率（recall）**：attack / return / search / next / end_battle 各自的
   检出数 ÷ 期望数。这是「模型能否识别状态转换标志」的直接答案。
3. **资源建筑检出**：战斗/搜索/村庄场景检测到敌方资源建筑的帧数。
4. **完整转换链成功率**：`--sequence` 模式下 FSM history 是否等于
   `[村庄待机, 搜索中, 战斗中, 战斗结束, 村庄待机]`。
5. **捐兵场景**：识别出「请求条目」和「增援按钮」的帧比例
   （扩充类别后评估）。

### 6.2 验收门槛（建议）
- 打资源 6 场景：状态准确率 ≥ 80%（当前 OCR 参考 100%，模型待训练后验证）；
- 4 类核心按钮检出率 ≥ 60%（数据扩充前）；
- 数据扩充后 ≥ 80%。

## 7. 端到端闭环测试（下一阶段）

在 Simulator（executor/simulator.py）与真机两条链路验证：
1. **值得/不值得决策命中率**：资源评估算法结果 vs 实际战斗收益（可抢资源 > 出兵成本）。
2. **动作成功率**：FSM 输出动作 → ActionValidator 校验 → 真机点击；无效/越界动作拦截率。
3. **循环收敛**：连续 N 个村庄后所有资源满 → STOP（无死循环）。
4. **成本下降**：级联统计（/cascade/stats）LLM 触发率随本地模型微调而下降，
   验证「先大模型 → 回流 → 常规分类器 → 表现下降再回大模型」闭环。

## 8. 待办清单
- [ ] 用户补充：战斗进行中/战斗结束/结算界面截图（各 ≥20 帧）
- [ ] 用户补充：捐兵请求详情/增援确认/增援完成截图（各 ≥20 帧）
- [ ] 跑通 5 的三步训练流程，产出 runs/detect/fsm 权重
- [ ] 用 6 的测试命令验收并记录指标到 PROGRESS.md
- [ ] 阶段 10 捐兵 FSM（消息列表→请求→增援按钮→自动兵种）
- [ ] 真机 ADB/无障碍动作执行 + /execute、/verify 端点
