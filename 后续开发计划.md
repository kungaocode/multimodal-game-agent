# 后续开发计划（打资源 / 捐兵 · 本地 FSM 识别器 · 回流微调闭环）

> 2026-09-02 · 承接 `状态转换识别测试计划.md` 的测试细节，本文是面向执行的项目计划：
> 目标 → 架构 → API 配置 → 数据 → 训练 → 测试验收 → 里程碑 → 用户待办。
> 全程中文；质量门槛：`pytest tests -ra` 全绿 + `python -m security.audit` 通过。

---

## 1. 背景与现状（2026-09-02 快照）

| 项目 | 现状 |
|------|------|
| 43 类检测模型 | ✅ `runs/detect/train/weights/best.pt`（基线，已训练） |
| ResNet18 等级分类器 | ✅ `runs/classifier/best.pt` |
| 资源优先度算法 | ✅ `decision/resource_policy.py`（价值 0.40 + 可达 0.30 + 安全 0.20 + 距离 0.10） |
| 打资源 FSM | ✅ `decision/farm_fsm.py` + 新增 `decision/battle_fsm.py`（含搜索环节，BattleSignals） |
| 12 类 FSM 标签体系 | ✅ `perception/fsm_labels.py` |
| 级联感知（本地优先 + 大模型兜底 + 样本回流） | ✅ `perception/cascade.py` + `/cascade/stats` |
| qwen3-vl-plus 真实 API 联调 | ✅ `perception/vision_model.py`（JSON Output / 400 降级重试） |
| 回流样本 → YOLO 标注 | ✅ `tools/labeling/llm_samples_to_yolo.py`（已产出 `dataset/llm_finetune`：2 图 9 框） |
| OCR 半自动按钮标注 | ✅ `tools/labeling/ocr_buttons_to_yolo.py`（已产出 `dataset/fsm_real_buttons`：9 图 29 框） |
| 12 类合并微调脚本 | ✅ `tools/labeling/finetune_fsm.py` |
| 状态转换识别测试工具 | ✅ `tools/eval_state_transition.py`（单帧状态 + 信号召回 + `--sequence` FSM 链） |
| 基线测试 | ✅ 134 测试通过 / 安全审计通过 |
| farm 8 类基线训练 | 🔄 进行中（CPU，约 105 分钟，40 epochs） |

**关键事实（决定后续方案）**：合成集 `farm_synthetic` 只有 6 类资源建筑，按钮类样本为 0，
这是「本地模型识别不出状态转换标志」的直接原因 → 后续把按钮类真实标注（OCR + LLM 回流 + 手工）
合并进 12 类体系微调，本地模型只负责当 FSM 的「信号识别器」。

---

## 2. 总体架构（用户想法的工程化落地）

```
                    ┌────────────────────────────────────────────┐
                    │                级联感知 Cascade              │
  截图 ──► 本地 FSM 识别器（YOLO 12 类，轻量、毫秒级、零成本、可离线）│
                    │   ├─ 信号齐全 & 置信度达标 ──► FSM 状态机流转   │
                    │   └─ 无建筑/置信度<阈值/场景关键信号缺失          │
                    │        └─► qwen3-vl-plus 兜底（仅这部分花钱）   │
                    │              └─ 成功样本自动回流 llm_samples/   │
                    └────────────────────────────────────────────┘
                                     │ 回流
                                     ▼
               tools/labeling/llm_samples_to_yolo.py（教师→学生蒸馏）
                                     ▼
               tools/labeling/finetune_fsm.py（12 类微调，本地识别器变强）
                                     ▼
               表现下降到不可接受时 ──► 提高兜底触发率，重新依赖大模型
```

- **本地模型不用强**：只输出 12 类信号框（6 资源建筑 + 6 按钮），状态判断交给 FSM。
- **成本闭环**：大部分帧走本地（0 成本），LLM 只处理本地拿不准的帧；
  每轮微调后 LLM 触发率应下降，`/cascade/stats` 持续可观测。
- **回退机制**：新增真实场景帧进验证集，评估曲线下滑或按钮召回 < 门槛时，
  把 Cascade 置信度阈值调高 / 切换 local→auto，重新引入大模型。

---

## 3. API 依赖：需要你申请/配置的部分

> 结论先行：**qwen3-vl-plus 符合要求**（多模态视觉，支持 OpenAI compatible-mode 与 JSON Output，
> 本项目已真实联调通过）。你已开通的 **api.txt 中第 2 个 key** 就是用于视觉大模型的密钥。

本项目调用阿里云百炼「视觉大模型」只需要 **3 个配置**（均已封装在 `perception/vision_model.py`，
只读环境变量，不会写进代码/日志）：

| 环境变量 | 值（示例） | 说明 |
|---------|-----------|------|
| `VISION_MODEL_BASE_URL` | `https://dashscope.aliyuncs.com/compatible-mode/v1` | 百炼 compatible-mode 网关 |
| `VISION_MODEL_API_KEY` | `<api.txt 中第 2 个 sk-...>` | 你已开通的那个 key |
| `VISION_MODEL_NAME` | `qwen3-vl-plus` | 模型名（可换 qwen-vl-max 等） |

可选：`VISION_TRUST_ENV=1` —— 只有需要走系统代理访问外网时才设置
（默认不读环境代理，避免本机 socks5 代理把 httpx 打崩）。

**被调用的代码位置（4 处）**：
1. `perception/vision_model.py`：`describe()`（结构化 JSON 感知）/ `chat()`（高层规划）。
2. `perception/cascade.py`：级联兜底 — 本地置信度不足时自动调上述 `describe()`。
3. `api/main.py` `/perceive`、`/perceive/base64`：可选开启 LLM 计划。
4. `api/planner.py` `/plan`：规则层打分 + 可选 Qwen-VL 高层计划。

**启动方式示例**（终端 2 个窗口）：
```bash
export VISION_MODEL_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
export VISION_MODEL_API_KEY=sk-你的第二个key        # 从 api.txt 读取，勿外泄
export VISION_MODEL_NAME=qwen3-vl-plus
source activate multimodal-agent
# 窗口1：uvicorn api.main:app --port 8000
# 窗口2：python -c "from perception.vision_model import VisionModel; \
#         r=VisionModel().describe('final_test_dataset/018FC1FEC651489E0163B3D1862C3FB6.jpg'); print(r.json)"
```
安全要求：`api.txt` 已加入 `.gitignore`，密钥绝不入库、绝不进日志（`security.audit` 会持续把关）。

---

## 4. 数据方案（现状 → 目标）

### 4.1 现状（final_test_dataset，9 张真实截图）
| 状态 | 张数 | 覆盖 |
|------|------|------|
| 村庄待机 | 2 | 进攻按钮 |
| 搜索中 | 2 | 搜索对手 / 搜索动画 · 回营 |
| 战斗中 | 2 | 开战倒计时 / 战利品 / 下一个 / 结束战斗 |
| 捐兵-消息列表 | 3 | 请求增援 / 增援按钮 |
| **缺失** | — | 战斗进行中（下兵后）、战斗结束、胜利/失败结算、捐兵详情页、增援确认、增援完成反馈 |

### 4.2 扩充来源（量小的问题这样解决）
1. **手动截图**：按缺失环节补拍，每环节 ≥ 20 帧（不同村庄、不同分辨率、不同兵种）。
2. **OCR 半自动标注**：`ocr_buttons_to_yolo.py` 用 PaddleOCR 定位文字 → 自动生成按钮框，
   秒级扩充「按钮类」真值（已对 9 张图产出 29 个按钮框）。
3. **LLM 回流**：`/perceive?mode=auto` 跑真实流程时，本地拿不准的帧自动问 qwen3-vl-plus，
   成功样本自动落 `dataset/llm_samples/` → 转 YOLO 标注（教师-学生蒸馏 + 主动学习）。
4. **人工校正**：`tools/labeling/app.py` 复核/修正按钮与资源框。

### 4.3 类别平衡目标
- 按钮类（6 类）：以真实标注为主，每类 ≥ 20 框后进入稳定训练；
- 资源建筑（6 类）：继续用合成集扩充场景多样性（背景负样本足够）；
- 验证集以真实截图为主（打资源 6 张 + 捐兵 3 张起步）。

---

## 5. 训练方案（回流微调 → 12 类 FSM 识别器）

```bash
# ① 回流样本 → YOLO（已跑完：dataset/llm_finetune，2 图 9 框）
python -m tools.labeling.llm_samples_to_yolo --samples dataset/llm_samples --out dataset/llm_finetune

# ② OCR 半自动按钮标注（已跑完：dataset/fsm_real_buttons，9 图 29 框）
ALL_PROXY= HTTPS_PROXY= HTTP_PROXY= \
python -m tools.labeling.ocr_buttons_to_yolo \
    --images final_test_dataset dataset/game_picture --out dataset/fsm_real_buttons

# ③ 合并 6 类合成 + 回流 + OCR 按钮 → 12 类微调
python -m tools.labeling.finetune_fsm \
    --synthetic dataset/farm_synthetic \
    --llm dataset/llm_finetune \
    --buttons dataset/fsm_real_buttons \
    --base runs/detect/farm/weights/best.pt --epochs 20 --out runs/detect/fsm
```

- **权重路径链**：`yolov8n.pt`（预训练）→ `runs/detect/farm/weights/best.pt`（资源 8 类基线，训练中）
  → `runs/detect/fsm/weights/best.pt`（12 类 FSM 识别器，**最终产物**）。
- 若 farm 基线未完成可先用 `--base yolov8n.pt` 快速出 FSM 权重。
- 轻量训练（≤25 epochs，imgsz 512~640），CPU 每 epoch 约 2~3 分钟。

---

## 6. 状态转换识别测试（工具已实现 + 基线结论）

```bash
# 打资源 + 捐兵场景信号识别：模型权重 + OCR 参考
python tools/eval_state_transition.py --images final_test_dataset \
    --weights runs/detect/fsm/weights/best.pt --ocr

# 完整状态转换链（村庄→搜索→战斗→结束→返回）：按实际顺序截图序列驱动 BattleFSM
python tools/eval_state_transition.py --images final_test_dataset \
    --sequence 1.jpg,A8B537A65DA5ED009F57C5B48618D349.jpg,018FC1FEC651489E0163B3D1862C3FB6.jpg \
    --weights runs/detect/fsm/weights/best.pt --json /tmp/seq_report.json
```

### 6.1 基线结果（43 类模型 + OCR，9 张真实截图，2026-09-02）
| 指标 | 结果 | 结论 |
|------|------|------|
| 打资源状态准确率（6 张） | **6/6 = 100%** | 信号规则 + OCR 已能判状态 |
| 按钮信号召回（attack/search/next/end/return） | **8/8 = 100%** | 状态转换标志可被识别 |
| 敌方资源建筑检出 | **0/6** | 43 类模型不认识真实战斗场景的敌方建筑（合成 vs 真实域差异）→ 12 类微调的核心攻坚点 |
| 捐兵场景（3 张） | 属捐兵 FSM | 识别出「请求条目 + 增援按钮」= 命中，后续单独验收 |

### 6.2 验收门槛（FSM 微调完成后）
- 打资源 6 场景状态准确率 ≥ 80%；
- 4 类核心按钮召回 ≥ 60%（数据扩充前）/ ≥ 80%（扩充后）；
- 敌方资源建筑检出：战斗/搜索场景 ≥ 50% 帧检出至少 1 个敌方资源；
- `--sequence` 完整链 = `[村庄待机, 搜索中, 战斗中, 战斗结束, 村庄待机]`。

---

## 7. 打资源完整流程测试（端到端闭环）

用户叙述 → 状态机（`decision/battle_fsm.py` 已实现）：

```
村庄待机 ─ OCR 顶栏查自己资源（金/圣/黑油）
  ├─ 全满 → STOP
  ├─ 识别「进攻按钮」→ 点进攻 → 常规战搜索 ─► 搜索中
搜索中  ─ 识别敌方界面 + 可获得战利品
  ├─ resource_policy 评估「值不值得」
  │   ├─ 值得 → 点击放兵（特定兵种放资源处）→ 开战 ─► 战斗中
  │   └─ 不值得 → 点「下一个」→ 继续搜索（保持搜索中）
  └─ 识别「回营」→ 返回村庄待机
战斗中  ─ 打掉目标建筑（罐子/采集器，距离/防御因素可放弃若干）
  ├─ 识别「结束战斗」→ 战斗结束
  └─ 敌资源打光 / 兵力用尽 → 战斗结束 ─► 战斗结束
战斗结束 ─ 识别「返回」→ 村庄待机（循环检测资源）
```

**测试方法**：
1. **Simulator 闭环**（`executor/simulator.py`）：确定性仿真验证 FSM 逻辑
   ——值得/不值得、超步数、资源全满 STOP、无效动作拦截；
2. **真实帧驱动**：用 final_test_dataset + 新增战斗/结算截图驱动 BattleFSM，
   逐状态校验识别→决策→动作；
3. **真机 ADB/无障碍**（阶段 15）：动作执行器点击真实 UI，前后帧状态差异验证。

**指标**：值得决策命中率 ≥ 80%（可抢资源 > 出兵成本）；动作校验拦截率（无效/越界应为 0 漏网）；
循环收敛（N 个村庄后全满 STOP，无死循环）。

---

## 8. 捐兵模块流程测试（阶段 10）

用户叙述 → 捐兵 FSM：

```
打开部落面板 → 展开左边「消息列表」→ 搜索「请求援兵」条目
  有请求 → 点开 → 识别「增援」（绿色）→ 点击
      用户指定兵种 → 捐指定兵种
      未指定 → 自动选默认兵种（如气球兵）
  无请求/完成 → 关闭消息列表 → 返回主界面
```

**感知验收（本计划先做）**：识别「请求条目」+「增援按钮」的帧比例；
现 3 张捐兵截图已能 OCR 出「救命啊！我需要增援！」+ 增援按钮。
**动作验收（后续）**：捐兵 FSM 状态机 + 真机点击；增援后校验「已增援」/战利品反馈。
**类别扩充建议**：消息列表、请求条目、增援按钮、关闭按钮 4 类（并入 fsm_labels 或独立 DonationFSM）。

---

## 9. 里程碑与排期（建议）

| 里程碑 | 内容 | 验收 |
|--------|------|------|
| **M1（本周）** | farm 基线训练完成 → 12 类 FSM 微调 → 状态转换测试 | 6.2 的门槛达标；指标记入 PROGRESS.md |
| **M2（下周）** | 数据扩充（战斗进行中/结束/结算各 ≥20 帧）+ 打资源端到端 Simulator 闭环 | 闭环成功率 + 决策命中率报告 |
| **M3** | 捐兵 FSM + 消息列表 4 类识别 + 自动兵种（默认气球兵） | 捐兵流程闭环 |
| **M4** | 真机 ADB/无障碍执行 + `/execute` `/verify` + 成本闭环统计 | LLM 触发率下降曲线、安全审计通过 |
| **M5** | Android / HarmonyOS 客户端（阶段 11/12） | 真机验收 |

---

## 10. 需要你配合的事项（待办清单）

- [ ] **API**：把 `api.txt` 第 2 个 key 配置为 `VISION_MODEL_API_KEY`（其余 2 个变量见第 3 节）；
      如有多个百炼 key，请确认第 1 个 key 用途（当前代码只用第 2 个）。
- [ ] **确认 qwen3-vl-plus**：符合要求（已联调通过），无需更换。
- [ ] **授权长时训练**：farm 基线 + FSM 微调均为 CPU 长任务（各约 1~2 小时），
      需在非沙箱环境后台运行，避免被回收中断。
- [ ] **截图扩充**：战斗进行中 / 战斗结束 / 结算界面 / 捐兵详情页 / 增援确认 / 增援完成，
      每环节 ≥ 20 帧，放入 final_test_dataset（或新目录后更新 labels.json）。
- [ ] **提供真实运行环境**：打资源/捐兵端到端测试需要模拟器或真机（ADB）。
- [ ] **标注复核**：`tools/labeling/app.py` 人工过一遍 OCR/LLM 生成的按钮框（29 框，工作量小）。

---

## 附：相关文件索引
| 文件 | 作用 |
|------|------|
| `状态转换识别测试计划.md` | 测试细节（信号表、数据清单、指标定义） |
| `decision/battle_fsm.py` | 含搜索环节的打资源状态机（BattleFSM/BattleSignals） |
| `perception/fsm_labels.py` | 12 类 FSM 标签体系 + OCR 文本映射 |
| `perception/cascade.py` | 级联路由（本地优先 + 大模型兜底 + 回流） |
| `perception/vision_model.py` | qwen3-vl-plus 封装（compatible-mode + JSON Output） |
| `tools/labeling/{llm_samples_to_yolo,ocr_buttons_to_yolo,finetune_fsm}.py` | 回流/OCR/合并微调工具 |
| `tools/eval_state_transition.py` | 状态转换识别测试 |
| `final_test_dataset/labels.json` | 9 张真实截图 ground truth |
