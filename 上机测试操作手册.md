# 上机测试操作手册（手机直连 · 局域网 · Coach 主动采集）

> 2026-09-03 · 适用于「不录屏、直接上机、强模型教练引导、会话自动采数」的路径
> 配合 `打资源测试与蒸馏训练计划.md` 使用。

## 0. 当前已就绪（后端已在本机启动）

| 项 | 值 |
|---|---|
| 后端地址（手机访问） | `http://192.168.31.250:8000` |
| 控制台页面 | `http://192.168.31.250:8000/static/index.html` |
| 视觉大模型（Qwen 教练） | ✅ 已配置（qwen3-vl-plus，api.txt 第 2 个 key） |
| 本地模型（当前生效） | 检测器 `runs/detect/fsm_v4/weights/best.pt` ＋ 状态分类器 v4 |
| 级联模式 | `CASCADE_MODE=auto`（本地优先 ＋ LLM 兜底 ＋ 样本回流） |
| 会话记录 | `dataset/coach_sessions/<时间戳>/`（截图＋本地输出＋教练输出） |

> 安全提醒：服务监听 `0.0.0.0:8000`，**不要暴露到公网**；只有选择「ADB 执行」时才会真实点击手机。
> ⚠️ 2026-09-03 深晚：第6轮已暂停待修复（下兵逻辑/区域选择/结束判定）；当前 ADB 未连接，测试前先按第 1 节重新配对连接。

## 1. 手机与电脑建立 ADB 连接（二选一）

### 方法 A：Android 11+「无线调试」（推荐）

1. 手机开启「开发者选项」→ 打开「无线调试」；
2. 手机与电脑连同一个 Wi-Fi（本机在 `192.168.31.250`，手机应为 `192.168.31.x`）；
3. 无线调试 → 「使用配对码配对设备」：记下 **配对地址 `<IP>:<配对端口>`** 和 **6 位配对码**；
4. 电脑上执行配对（配对码只在一两分钟内有效）：

   ```bash
   tools/android-platform-tools/platform-tools/adb pair <手机IP>:<配对端口> <配对码>
   ```

5. 记下无线调试界面里的 **「IP 地址和端口」`<手机IP>:<连接端口>`**，电脑执行连接：

   ```bash
   tools/android-platform-tools/platform-tools/adb connect <手机IP>:<连接端口>
   ```

6. 手机弹窗「允许调试？」→ 勾选「始终允许」；
7. 确认已连接：

   ```bash
   tools/android-platform-tools/platform-tools/adb devices   # 应显示 device
   ```

### 方法 B：USB 转无线（老版本安卓 / 不方便用配对码时）

```bash
# 1) USB 连接手机，开启「USB 调试」
tools/android-platform-tools/platform-tools/adb devices      # 先确认 USB 在线
tools/android-platform-tools/platform-tools/adb tcpip 5555   # 打开 5555 端口
# 2) 拔掉 USB，改为局域网连接（手机 IP 见 设置→关于手机→状态信息）
tools/android-platform-tools/platform-tools/adb connect <手机IP>:5555
tools/android-platform-tools/platform-tools/adb devices
```

> 手机 IP 可用手机「无线调试」界面查看，或在已 USB 连接时执行：
> `adb shell ip -f inet addr show wlan0 | grep inet`

## 2. 把游戏打开到正确界面

- 手机打开「部落冲突」，**进到村庄界面**（不要停在登录/加载/商店页）；
- 建议先手动把手机屏幕常亮（或稍后设置 step_delay 足够大）。

## 3. 手机浏览器跑「打资源」会话（控制台方式）

1. 手机浏览器打开 `http://192.168.31.250:8000/static/index.html`；
2. 后端地址填：`http://192.168.31.250:8000`；页面会显示模型/教练/ADB 状态；
3. 模块：**打资源**；运行方式：**ADB 执行**；
4. serial 填：**`<手机IP>:<连接端口>`**（与第 1 步连接一致）；
5. 先小步试跑：max_steps=10、step_delay=2.0、启用教练；
6. 点「启动」→ 页面轮询 FSM 状态、动作、Qwen 教练批改；
7. 需要中止时点「停止」。

## 4. 等价命令行（电脑上直跑，效果相同）

```bash
cd /home/kun/Desktop/app_for_Harmony_Android
source activate multimodal-agent
export VISION_MODEL_BASE_URL='https://dashscope.aliyuncs.com/compatible-mode/v1'
export VISION_MODEL_NAME='qwen3-vl-plus'
export VISION_MODEL_API_KEY="$(sed -n '2p' api.txt)"

python -m tools.coach_session \
  --module farm --serial <手机IP>:<连接端口> \
  --max-steps 20 --step-delay 2.0 \
  --weights runs/detect/fsm_v4/weights/best.pt
```

每步会记录 `step_XXXX_screenshot.jpg` + `step_XXXX_local.json` + `step_XXXX_coach.json`；
教练批改的帧就是「弱模型错、强模型对」的最高价值训练样本 —— **数据是会话自动采集的，不需要录屏**。

## 5. 会话回流与重训（主动学习闭环）

1. 跑若干轮会话（建议覆盖 村庄待机→搜索→战斗→结束→返回 各状态，≥ 3~5 轮）；
2. 控制台「回流训练」区域发起重训（或 `POST /session/retrain`），产出：
   - 检测器：`runs/detect/fsm_v6/weights/best.pt`
   - 状态分类器：`runs/state_classifier_v6/best.pt`
3. 用 `tools/eval_state_transition.py`（独立评测集）验收通过后，重启后端并切换权重：

   ```bash
   export DETECTOR_MODEL='runs/detect/fsm_v6/weights/best.pt'
   export STATE_CLASSIFIER_MODEL='runs/state_classifier_v6/best.pt'
   ```

4. 循环以上过程：教练指导程度按 `打资源测试与蒸馏训练计划.md` 阶段 C 渐进下调
   （教练抽样率 100%→50%→只审分歧→只审报错；级联阈值 0.5→0.4→0.3）。

## 6. 故障排查

| 现象 | 处理 |
|---|---|
| 手机打不开控制台 | 确认同一 Wi-Fi；`sudo ufw allow 8000/tcp`（若开防火墙）；换端口（如 18000）重启后端 |
| `adb devices` 显示 offline/unauthorized | 手机弹窗点「始终允许」；重新 pair + connect |
| 配对超时 | 配对码 1~2 分钟过期，重新生成 |
| 教练不工作 | 确认 `VISION_MODEL_API_KEY` 已配置；`/session/capabilities` 中 `coach_configured=true` |
| 会话只 wait 不点击 | 游戏不在村庄界面 / 本地模型未检出按钮，教练批改后会纠正——先确认第 2 步 |
