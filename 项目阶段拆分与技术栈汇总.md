# 多模态游戏 AI Agent 实验平台 —— 阶段拆分与技术栈汇总

> **项目定位**：构建一个基于多模态大模型的游戏环境感知、任务规划、行为决策与执行实验平台。
> 目标场景：自动识别《部落冲突》等游戏中的各类建筑，并实现打资源（farm）、升级（upgrade）、捐兵（donation）等基础行为。
> 最终交付：可在 **Android / HarmonyOS** 上运行的 APK 应用。
>
> **适用范围声明**：本项目用于受控实验环境、离线截图或自建游戏模拟环境，不针对真实在线游戏实施自动化操作，不研究绕过游戏反作弊系统的方法。反作弊检测相关内容仅作为 AI Agent Security / Bot Detection 的理论研究方向。

---

## 目录

1. [项目目标与核心思路](#一项目目标与核心思路)
2. [总体开发路线](#二总体开发路线)
3. [核心架构与数据流](#三核心架构与数据流)
4. [阶段拆分总览表](#四阶段拆分总览表)
5. [各阶段详细说明](#五各阶段详细说明)
   - [阶段 0：项目准备与环境搭建](#阶段-0项目准备与环境搭建)
   - [阶段 1：视觉识别模块](#阶段-1视觉识别模块)
   - [阶段 2：状态管理与 FSM](#阶段-2状态管理与-fsm)
   - [阶段 3：资源任务模块（打资源）](#阶段-3资源任务模块打资源)
   - [阶段 4：资源目标优先度算法](#阶段-4资源目标优先度算法)
   - [阶段 5：升级任务模块](#阶段-5升级任务模块)
   - [阶段 6：接入国产多模态大模型](#阶段-6接入国产多模态大模型)
   - [阶段 7：Agent 闭环](#阶段-7agent-闭环)
   - [阶段 8：行为执行器与模拟环境](#阶段-8行为执行器与模拟环境)
   - [阶段 9：后端 API 封装（FastAPI）](#阶段-9后端-api-封装fastapi)
   - [阶段 10：捐兵模块（后续模块）](#阶段-10捐兵模块后续模块)
   - [阶段 11：Android 客户端集成](#阶段-11android-客户端集成)
   - [阶段 12：HarmonyOS 客户端集成](#阶段-12harmonyos-客户端集成)
   - [阶段 13：实验评估体系](#阶段-13实验评估体系)
   - [阶段 14：AI Agent 安全研究](#阶段-14ai-agent-安全研究)
   - [阶段 15：真机测试与优化、打包交付](#阶段-15真机测试与优化打包交付)
6. [技术栈汇总表](#六技术栈汇总表)
7. [MVP（第一版最小可用版本）范围](#七mvp第一版最小可用版本范围)
8. [核心设计原则](#八核心设计原则)
9. [学习路线对照](#九学习路线对照)

---

## 一、项目目标与核心思路

**核心技术链：**

```
截图/视觉输入
    ↓
视觉识别
    ↓
结构化游戏状态
    ↓
规则/优先度决策
    ↓
多模态大模型推理
    ↓
任务规划
    ↓
行为执行
    ↓
再次观察
    ↓
状态验证与下一步决策
```

**核心思想：** 不要先做「游戏脚本」，而是先做「AI Agent 实验平台」。这样能完整保留真正要学习的能力：

> 计算机视觉 + 多模态大模型 + Agent + 软件工程 + 移动端 + AI Security

**三大行为目标（对应三个任务模块）：**

| 行为 | 对应模块 | 说明 |
|------|----------|------|
| 打资源 | `ResourceTask` | 识别资源目标 → 评估 → 选择 → 规划 → 执行 → 验证 |
| 升级 | `UpgradeTask` | 检测工人 → 扫描可升级建筑 → 算优先级 → 规划 → 执行 → 验证 |
| 捐兵 | `DonationTask` | 打开城堡 → 识别请求 → 解析需求 → 匹配可用兵种 → 规划 → 验证 |

**最终愿景：** 形成完整的研究/工程项目 ——「基于多模态大模型的跨平台游戏环境感知与安全智能任务规划系统」。

---

## 二、总体开发路线

推荐采用 **「Linux 后端研发 → 模拟环境测试 → 移动端集成 → 真实设备测试」** 的路线：

```
阶段 1-8（Linux）   完成绝大多数核心模块（视觉/状态/决策/Agent/执行器）
阶段 9              将成熟模块封装为 API / SDK
阶段 10-12          集成 Android / HarmonyOS 客户端
阶段 13-15          真机功能、性能、稳定性、安全测试与交付
```

**为什么这样做：**

1. Linux 环境更适合 Python、CV、模型推理、日志和自动化测试。
2. 核心算法与移动端 UI / 系统 API 完全解耦。
3. 出现问题时能快速定位故障来源（AI / 算法 / 网络 / 移动端）。
4. 先完成一个可复现的实验系统，再做跨平台部署。

---

## 三、核心架构与数据流

### 3.1 系统整体架构

```
Mobile App (Android / HarmonyOS)
        ↓ 截图 / 图像采集 / 用户输入 / 状态展示
API / SDK (FastAPI REST / WebSocket)
        ↓
Agent Backend (决策引擎 / 规划器 / 工具调用)
        ↓
Perception（视觉 / OCR）→ State Manager（结构化状态 / FSM）
        ↓
Decision Engine（规则打分 + LLM 推理）
        ↓
Action Validator（安全校验）→ Executor（模拟器 / 移动端执行器）
        ↓
Environment → Feedback / Observation → 循环
```

### 3.2 推荐后端项目目录结构

```
multimodal-game-agent/
├── perception/          # 视觉识别
│   ├── detector.py      #   目标检测
│   ├── ocr.py           #   OCR 文字识别
│   └── vision_model.py  #   多模态视觉模型
├── state/
│   ├── game_state.py    #   游戏状态数据结构
│   └── state_manager.py #   状态管理器 / FSM
├── reasoning/
│   ├── llm.py           #   大模型 API 调用
│   ├── planner.py       #   任务规划器
│   └── prompt.py        #   提示词管理
├── decision/
│   ├── resource_policy.py   # 资源目标打分
│   ├── upgrade_policy.py    # 升级优先度打分
│   └── priority.py          # 通用优先度算法
├── tasks/
│   ├── resource.py      #   打资源任务
│   ├── upgrade.py       #   升级任务
│   └── donation.py      #   捐兵任务（后续）
├── executor/
│   ├── action.py        #   结构化动作定义
│   └── simulator.py     #   模拟环境执行器
├── security/
│   ├── validator.py     #   动作校验
│   ├── risk.py          #   风险评分
│   └── policy.py        #   权限策略
├── evaluation/
│   ├── accuracy.py      #   识别准确率
│   ├── success_rate.py  #   任务成功率
│   └── latency.py       #   延迟指标
├── config/              # 配置文件
├── tests/               # 自动化测试
└── README.md
```

---

## 四、阶段拆分总览表

| 阶段 | 名称 | 核心目标 | 关键交付物 |
|------|------|----------|------------|
| 0 | 项目准备与环境搭建 | Linux 开发环境、数据集准备、工程骨架 | 环境就绪 + 50~200 张离线截图 |
| 1 | 视觉识别模块 | 截图 → 结构化信息 | 检测器 / OCR / 结构化 Game State |
| 2 | 状态管理与 FSM | 建立「当前环境状态」 | State Manager + FSM |
| 3 | 资源任务模块（打资源） | 模拟环境完成打资源闭环 | ResourceTask 模块 |
| 4 | 资源目标优先度算法 | 规则打分选目标 | Priority 打分算法 |
| 5 | 升级任务模块 | 模拟环境完成升级闭环 | UpgradeTask 模块 |
| 6 | 接入国产多模态大模型 | LLM 承担高层规划 | LLM 接入 + 结构化 JSON 输出 |
| 7 | Agent 闭环 | 感知→推理→规划→行动→验证 | 完整 Agent Loop |
| 8 | 行为执行器与模拟环境 | 动作校验 + 模拟执行 | Action Validator + Simulator |
| 9 | 后端 API 封装 | 核心能力封装成 API/SDK | FastAPI 服务 |
| 10 | 捐兵模块（后续） | 需求解析与兵种匹配 | DonationTask 模块 |
| 11 | Android 客户端集成 | 移动端调用后端 | Android APK（Beta） |
| 12 | HarmonyOS 客户端集成 | 跨平台客户端 | HarmonyOS 应用 |
| 13 | 实验评估体系 | 量化指标度量系统 | 评估脚本 + 指标报告 |
| 14 | AI Agent 安全研究 | 安全防护与检测研究 | Security Layer |
| 15 | 真机测试与交付 | 稳定性 / 性能 / 打包 | 正式 APK |

> 💡 **里程碑提醒**：阶段 0~8 完成即为 **MVP（第一版最小可用版本）**，详见 [第七节](#七mvp第一版最小可用版本范围)。

---

## 五、各阶段详细说明

### 阶段 0：项目准备与环境搭建

**目标**：搭建完整 Linux 研发环境，准备离线测试数据集，建立工程骨架。

**任务清单：**
- 安装 Ubuntu Linux + Python 虚拟环境（venv / Conda）
- 配置 Git / GitHub 仓库、VS Code 开发环境
- 安装基础依赖：OpenCV、PyTorch、Transformers、FastAPI、pytest
- 准备 50~200 张离线游戏截图（受控实验 / 自建模拟环境）
- 建立建议的项目目录结构（见 §3.2）

**交付物：** 可运行的工程骨架 + 数据集目录 + Git 仓库

**验收标准：** `python -m pytest` 可跑通空测试；数据集可被后续脚本加载

**技术栈：** Ubuntu / Git / VS Code / Python / venv / Conda / OpenCV / PyTorch / pytest

---

### 阶段 1：视觉识别模块

**目标**：输入一张游戏截图，识别其中的关键对象并转换成结构化信息。

**需要识别的对象：**
- 资源建筑（金矿、圣水收集器、暗黑重油钻井等）
- 防御建筑（箭塔、加农炮、防空炮等）
- 生产建筑（兵营、训练营、法术工厂等）
- 工人（工人小屋，空闲/忙碌状态）
- 城堡（部落城堡）
- 关键 UI 元素（按钮、弹窗、菜单）
- 资源数量、建筑等级、当前任务状态

**结构化输出示例：**

```json
{
    "resources": {
        "gold": 850000,
        "elixir": 620000
    },
    "builders": {
        "total": 5,
        "idle": 2
    },
    "buildings": [
        {
            "type": "resource_storage",
            "level": 10,
            "position": [320, 240]
        }
    ]
}
```

**本阶段范围限制：** 仅使用固定截图做识别，**不进行任何自动操作**。

**评估指标：**
- Object Detection Accuracy（目标检测准确率）
- OCR Accuracy（文字识别准确率）
- State Extraction Accuracy（状态提取准确率）
- False Positive Rate（误报率）
- False Negative Rate（漏报率）

**技术栈：** OpenCV / CNN / YOLO / Faster R-CNN（理解原理）/ U-Net / Mask R-CNN（可选分割）/ PaddleOCR / Tesseract / EasyOCR

---

### 阶段 2：状态管理与 FSM

**目标**：建立 Game State Manager，让 Agent 不再依赖单张图片直接决定行为，而是首先建立「当前环境状态」。

**典型状态定义：**

| 状态 | 含义 |
|------|------|
| `GAME_NOT_FOUND` | 未检测到游戏环境 |
| `GAME_READY` | 环境已就绪 |
| `IDLE` | 空闲，可接受任务 |
| `RESOURCE_TASK` | 打资源任务执行中 |
| `UPGRADE_TASK` | 升级任务执行中 |
| `OBSERVING` | 观察中 |
| `ERROR` | 异常状态 |
| `RECOVERY` | 恢复流程 |

**核心原则：**
> 任何任务开始之前，必须先确认环境处于允许执行任务的状态。

实验环境可设计的状态：环境不存在 / 加载中 / 正常 / 异常 / 任务执行中。

**技术栈：** Python 状态机 / 自定义 State Manager / pydantic（状态数据结构校验）

---

### 阶段 3：资源任务模块（打资源）

**目标**：在自建 / 模拟游戏环境中完成打资源的完整闭环。

**模块结构：**

```
ResourceTask
├── detect_targets()    # 识别资源目标
├── evaluate_targets()  # 评估目标价值
├── select_target()     # 选择目标
├── generate_plan()     # 生成任务计划
├── execute_plan()      # 执行模拟动作
└── verify_result()     # 重新观察并验证结果
```

**流程：** 识别资源目标 → 计算目标价值 → 选择目标 → 生成任务计划 → 执行模拟动作 → 重新观察并验证结果。

**技术栈：** 阶段 1 视觉模块 + 阶段 2 状态模块 + 阶段 8 模拟执行器

---

### 阶段 4：资源目标优先度算法

**目标**：先使用传统规则打分，而不是让 LLM 完全负责决策。

**示例打分公式：**

```
Target Score =
    0.40 × Resource Value   （资源价值）
  + 0.30 × Accessibility    （可达性）
  + 0.20 × Safety           （安全性）
  + 0.10 × Distance         （距离）
```

**后续可加入的因子：** 资源价值 / 可达性 / 风险 / 距离 / 预计收益 / 行动成本。

**实验对比目标：**

| 方案 | 说明 |
|------|------|
| A. 纯规则系统 | 完全确定性打分 |
| B. 纯 LLM 决策 | LLM 直接选择 |
| C. 规则 + LLM 混合 | 规则筛选候选，LLM 做最终决策 |

**推荐采用方案 C。**

**技术栈：** Python 打分算法 / 配置驱动的权重参数 / pytest 单元测试

---

### 阶段 5：升级任务模块

**目标**：建立 UpgradeTask，在模拟环境完成升级闭环。

**流程：**

```
检测工人
  ↓
判断是否存在空闲工人
  ↓
扫描可升级建筑
  ↓
计算优先级
  ↓
选择候选目标
  ↓
生成任务计划
  ↓
执行模拟升级
  ↓
重新观察
  ↓
验证结果
```

**升级优先度公式示例：**

```
Upgrade Score =
    Building Importance
  × Strategic Value
  × Resource Efficiency
  × Urgency
```

**可加入因子：** 升级成本 / 升级时间 / 当前等级 / 战略价值 / 资源约束 / 当前任务需求。

**技术栈：** 阶段 1~4 模块复用 / priority 算法 / 模拟执行器

---

### 阶段 6：接入国产多模态大模型

**目标**：让多模态大模型承担高层语义理解、候选方案比较和任务规划。

**推荐架构：**

```
Screenshot
  ↓
Vision Model（视觉编码）
  ↓
Structured State（结构化状态）
  ↓
Rule-based Scoring（规则打分）
  ↓
Candidate Actions（候选动作）
  ↓
Multimodal LLM（多模态大模型）
  ↓
Structured Plan（结构化计划）
  ↓
Action Validator（动作校验）
  ↓
Simulator（模拟执行）
```

**模型要求：**
- 国产多模态模型为主，例如 **Qwen 系列**（Qwen-VL 等）
- 使用 OpenAI-compatible API
- 启用 Structured Output / JSON Output

**LLM 输出示例（尽量使用结构化 JSON，而非自然语言）：**

```json
{
    "task": "upgrade_building",
    "target": "building_A",
    "priority": 0.91,
    "confidence": 0.87,
    "reason": "High strategic value and affordable resource cost"
}
```

**技术栈：** Transformers / Qwen-VL API / OpenAI-compatible SDK / httpx / 结构化输出约束

---

### 阶段 7：Agent 闭环

**目标**：形成完整的感知 → 推理 → 规划 → 行动 → 观察 → 验证闭环。

**闭环流程：**

```
Perception（感知）
  ↓
Reasoning（推理）
  ↓
Planning（规划）
  ↓
Action（行动）
  ↓
Observation（观察）
  ↓
Verification（验证）
  ↓
Next Action（下一步动作）
```

**核心原则：**
> 执行之后必须重新观察，不能假设「我发出了动作，所以动作一定成功」。

**前后状态对比验证：**

```
Before State（执行前状态）
  ↓
Action（执行动作）
  ↓
After State（执行后状态）
  ↓
State Difference（状态差异）
  ↓
Success / Failure（成功 / 失败）
  ↓
失败 → 重新规划 或 进入安全状态
```

**可研究机制：** ReAct / Tool Calling / Task Planning / Reflection / Replanning / Memory（后期）。

**技术栈：** Agent 框架（自研或轻量框架）/ ReAct 模式 / 工具调用协议 / 结构化计划

---

### 阶段 8：行为执行器与模拟环境

**目标**：让 LLM 无法直接控制底层动作，所有动作必须经过校验与策略检查后由执行器执行。

**推荐架构：**

```
LLM
  ↓
Structured Action（结构化动作）
  ↓
Action Validator（动作校验）
  ↓
Permission / Policy Check（权限 / 策略检查）
  ↓
Executor（执行器）
  ↓
Environment（环境）
```

**动作示例：**

```json
{
    "action": "select_building",
    "target": "building_A"
}
```

**Validator 需判断：**
- 目标是否存在
- 当前状态是否允许操作
- 动作是否属于允许集合
- 是否需要人工确认
- 是否存在风险

**执行器分层（后期）：**
- `Simulator Executor` —— 第一阶段优先（自建模拟环境）
- `Android Executor` —— 移动端执行
- `HarmonyOS Executor` —— 移动端执行

**技术栈：** 动作 schema 定义 / pydantic 校验 / 权限策略模块 / 自建模拟器

---

### 阶段 9：后端 API 封装（FastAPI）

**目标**：把成熟的核心模块封装为 API / SDK，供移动端调用。

**架构：**

```
Mobile Client
  ↓
FastAPI（REST / WebSocket）
  ↓
AI Agent Backend
  ↓
Vision Model / LLM
```

**接口建议：**
- `POST /detect` —— 上传截图，返回结构化状态
- `POST /plan` —— 输入状态，返回任务计划
- `POST /execute` —— 执行结构化动作
- `POST /verify` —— 前后状态对比，验证结果
- `WS /stream` —— 实时任务状态推送（WebSocket）

**技术栈：** FastAPI / REST API / HTTP/HTTPS / JSON / WebSocket / uvicorn / pytest（接口测试）

---

### 阶段 10：捐兵模块（后续模块）

> ⚠️ 作为后续模块，**不需要在第一版 MVP 中实现**。

**模块结构：**

```
DonationTask
├── OpenCastle()          # 打开部落城堡
├── DetectRequests()      # 识别捐兵请求
├── ParseRequest()        # 解析请求需求
├── MatchAvailableUnits() # 匹配可用兵种
├── GeneratePlan()        # 生成计划
└── VerifyDonation()      # 验证捐赠结果
```

**重点研究：** OCR / Vision Understanding / 需求解析 / 可用资源匹配 / 任务验证。

**技术栈：** 阶段 1 视觉模块 + 阶段 6 多模态模型 + 阶段 8 执行器

---

### 阶段 11：Android 客户端集成

**目标**：在 Linux 后端稳定后，实现 Android 客户端，完成截图采集 → 调用后端 → 展示结果的链路。

**移动端主要职责：**
- UI
- 用户输入
- 截图 / 图像采集
- 状态展示
- 权限管理
- 网络通信
- 实验环境交互

**推荐架构：** Mobile App → API/SDK → Agent Backend（AI 与决策逻辑保持在独立模块，移动端只做交互）。

**技术栈：**
| 类别 | 技术 |
|------|------|
| 语言 | Kotlin / Java |
| IDE | Android Studio |
| UI | Jetpack Compose |
| 系统能力 | Activity / Service / Intent / Permission / Network / ADB |
| 与后端通信 | Retrofit / OkHttp / WebSocket |

**交付物：** Android APK（Beta 版）

---

### 阶段 12：HarmonyOS 客户端集成

**目标**：基于鸿蒙生态实现与 Android 对等的客户端，尽可能复用业务逻辑与 AI 接口。

**技术栈：**
| 类别 | 技术 |
|------|------|
| 语言 | ArkTS |
| UI | ArkUI |
| IDE | DevEco Studio |
| 系统能力 | Ability Kit / 权限体系 / 网络请求 / 数据存储 |
| 跨平台方向 | 研究 ArkUI-X，尽量复用业务逻辑与 AI 接口，平台相关能力单独封装 |

**交付物：** HarmonyOS 应用包（HAP）

---

### 阶段 13：实验评估体系

**目标**：项目不能只展示「能运行」，需要建立量化指标。

**四类指标：**

| 类别 | 指标 |
|------|------|
| 视觉 | Detection Accuracy / OCR Accuracy / State Accuracy |
| 决策 | Target Selection Accuracy / Upgrade Priority Accuracy / Planning Success Rate |
| Agent | Task Success Rate / Average Steps / Replanning Rate / Action Failure Rate / Latency |
| 系统 | CPU / GPU Usage / Memory Usage / Network Latency / API Cost |

**技术栈：** pytest / 评估脚本 / 日志系统 / 指标可视化（可选 Matplotlib / 图表）

---

### 阶段 14：AI Agent 安全研究

**目标**：由于 Agent 可以观察环境并调用工具，需要系统研究并防护各类安全风险。

**安全威胁清单：**
1. Prompt Injection（提示注入）
2. Tool Abuse（工具滥用）
3. 权限滥用
4. 敏感信息泄露
5. 错误决策
6. Agent Hijacking（Agent 劫持）
7. 不可信视觉输入

**Security Layer 架构：**

```
Security Layer
├── Action Validator（动作校验）
├── Risk Scoring（风险评分）
├── Permission Manager（权限管理）
├── Sensitive Data Filter（敏感数据过滤）
├── Audit Log（审计日志）
└── Human Confirmation（人工确认）
```

**置信度分级处置策略：**

```
confidence > 0.90        → 自动执行
0.60 ~ 0.90              → 再次观察 / 重新推理
confidence < 0.60        → 暂停并请求人工确认
```

**理论研究方向：** 反作弊系统的检测原理（客户端完整性 / 输入行为分析 / 行为模式分析 / 服务端异常检测）作为 Bot Detection 研究课题，**不研究绕过方法**。

**安全评估指标：**
- Invalid Action Block Rate
- High-risk Action Detection Rate
- Prompt Injection Detection Rate
- Unauthorized Tool Call Block Rate

**技术栈：** 动作校验器 / 风险评分引擎 / 权限系统 / 审计日志 / 敏感信息过滤器

---

### 阶段 15：真机测试与优化、打包交付

**目标**：真机上进行功能、性能、稳定性、安全测试，最终产出可安装的 APK。

**任务清单：**
- Android / HarmonyOS 真机功能测试（识别、打资源、升级链路）
- 性能测试：延迟、CPU / GPU / 内存占用、网络开销
- 稳定性测试：长时间运行、异常恢复、断网处理
- 安全测试：权限最小化、数据传输加密、审计日志核对
- 打包发布：正式签名 APK / HAP

**交付物：** 正式 APK + 安装部署文档 + 使用说明

---

## 六、技术栈汇总表

### 6.1 按层汇总

| 层级 | 技术栈 | 作用 |
|------|--------|------|
| **编程语言** | Python（主）/ C/C++（后期优化）/ Kotlin / Java / ArkTS / Shell | 核心研发与各端客户端 |
| **开发环境** | Ubuntu / Git / GitHub / VS Code / GCC / GDB / CMake / Docker / venv / Conda | 研发与自动化 |
| **计算机视觉** | OpenCV / CNN / YOLO / Faster R-CNN / U-Net / Mask R-CNN / SAM（可选） | 建筑、目标、UI 识别 |
| **多模态** | ViT / Vision Encoder / VLM / Qwen 系列 / Transformers | 复杂语义理解与跨模态推理 |
| **OCR** | PaddleOCR / Tesseract / EasyOCR | 资源数量、等级、任务文字 |
| **状态与决策** | 结构化 State / FSM / 规则打分 / Priority 算法 | 环境状态管理与确定性决策 |
| **大模型** | Qwen 系列 / OpenAI-compatible API / Structured Output | 高层规划、方案比较、行动解释 |
| **Agent** | ReAct / Tool Calling / Planning / Reflection / Replanning / Memory（后期） | 感知-推理-行动闭环 |
| **后端** | FastAPI / REST / WebSocket / JSON / uvicorn / httpx | 核心能力 API 化 |
| **模拟环境** | 自建 Simulator / Simulator Executor | 安全的执行与测试环境 |
| **Android** | Kotlin / Jetpack Compose / Activity / Service / Permission / Retrofit / ADB | 移动端客户端 |
| **HarmonyOS** | ArkTS / ArkUI / DevEco Studio / Ability Kit / ArkUI-X | 跨平台客户端 |
| **评估** | pytest / 指标脚本 / 日志系统 | 量化实验评估 |
| **安全** | Action Validator / Risk Scoring / Permission Manager / Audit Log | Agent 安全防护 |
| **部署** | Docker（后期）/ 虚拟环境 | 环境隔离与复现 |

### 6.2 最终技术链

```
Python + OpenCV + YOLO + PaddleOCR
  + ViT / VLM + Qwen 多模态大模型
  + FSM + Priority Algorithm
  + Agent / ReAct + Tool Calling
  + FastAPI + Simulator
  + Android + HarmonyOS（ArkUI-X）
  + Security + Evaluation
```

### 6.3 各阶段所需技术栈速查

| 阶段 | 核心技术栈 |
|------|------------|
| 0 | Ubuntu、Git、Python、OpenCV、PyTorch、pytest |
| 1 | OpenCV、YOLO、PaddleOCR、Tesseract、CNN |
| 2 | 状态机、pydantic、结构化数据 |
| 3 | 阶段 1+2 模块、模拟执行器 |
| 4 | 打分算法、权重配置、pytest |
| 5 | 阶段 1~4 模块、优先度算法 |
| 6 | Qwen-VL、Transformers、OpenAI-compatible API、JSON Output |
| 7 | ReAct、Tool Calling、Agent Loop |
| 8 | 动作 Schema、Validator、自建模拟器 |
| 9 | FastAPI、REST、WebSocket、uvicorn |
| 10 | OCR、多模态模型、执行器 |
| 11 | Kotlin、Jetpack Compose、Retrofit、ADB |
| 12 | ArkTS、ArkUI、DevEco Studio、ArkUI-X |
| 13 | pytest、日志、指标可视化 |
| 14 | 校验器、风险评分、权限、审计 |
| 15 | 真机调试、性能分析、签名打包 |

---

## 七、MVP（第一版最小可用版本）范围

> 不要一开始实现全部功能。第一版只完成以下 **11 项**：

1. Linux 环境搭建
2. 准备 50~200 张离线测试截图
3. 视觉模型识别目标
4. 转换成结构化 Game State
5. 实现 FSM
6. 实现资源目标优先度算法
7. 接入国产多模态模型 API
8. 输出结构化 Task Plan
9. 在自建模拟器执行动作
10. 重新截图并验证结果
11. 保存完整日志

**完成这一版之后**，再加入升级任务、捐兵模块、移动端和安全评估。

---

## 八、核心设计原则

1. **感知与决策分层**：视觉模块、状态模块、决策模块、执行模块相互解耦。
2. **LLM 不直接控制底层动作**：所有动作必须经过 `Validator → Policy Check → Executor`。
3. **规则优先，LLM 兜底**：先用确定性规则做大部分决策，LLM 负责高层语义与候选方案比较。
4. **执行后必须验证**：比较 Before / After State，失败则重新规划或进入安全状态。
5. **决策与执行解耦**：Simulator Executor / Android Executor / HarmonyOS Executor 可分别实现。
6. **移动端只做交互**：AI 与决策逻辑保持在独立模块中，移动端负责 UI、采集、通信、展示。
7. **先平台后移动**：Linux 版本稳定后再进入 Android / HarmonyOS。
8. **安全贯穿始终**：安全层不是后补功能，而是 Agent 架构的一部分。

---

## 九、学习路线对照

**研发阶段与技术学习顺序对照表：**

| 研发阶段 | 对应学习内容 |
|----------|--------------|
| 阶段 0~1 | Linux + Python + Git → OpenCV + CNN 基础 → YOLO + OCR |
| 阶段 2~3 | Vision Transformer → VLM / Multimodal LLM → Structured State |
| 阶段 4~5 | FSM → Rule-based Decision → Priority Algorithm |
| 阶段 6 | LLM API → Prompt Engineering → Structured Output → Tool Calling |
| 阶段 7 | AI Agent → ReAct → Planning → Observation-Action Loop → Reflection / Replanning |
| 阶段 8~9 | FastAPI → Simulator → Evaluation |
| 阶段 11~12 | Android → HarmonyOS → ArkUI-X → 移动端集成 |
| 阶段 14 | AI Agent Security → Prompt Injection → Tool Security → Risk Control |

**核心能力地图（七层）：**

| 层 | 问题 | 对应模块 |
|----|------|----------|
| 视觉层 | 看见什么？ | Perception |
| 状态层 | 当前发生了什么？ | State Manager |
| 决策层 | 什么最重要？ | Decision Engine |
| Agent 层 | 下一步应该做什么？ | Agent / Planner |
| 工具层 | 如何执行？ | Executor / Tool Calling |
| 反馈层 | 执行成功了吗？ | Feedback Loop |
| 安全层 | 这个动作允许执行吗？ | Security Layer |
| 评估层 | 系统到底做得怎么样？ | Evaluation |
