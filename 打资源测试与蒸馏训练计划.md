# 打资源测试与蒸馏训练计划（强模型指导 → 弱模型训练 → 渐进退场）

> 2026-09-03 更新
> 定位：打资源测试开始前的「进度汇总 + 后续计划」。
> 承接 `PROGRESS.md`（进度）、`后续开发计划.md`（开发计划）、`状态转换识别测试计划.md`（测试细节）。
> 核心路线（用户方案）：**先用强模型指导获取训练数据 → 进一步蒸馏训练产出最终模型 →
> 弱模型学会后渐进降低强模型指导程度，但强模型始终保留在场作为兜底**。

---

## 一、现状汇总（2026-09-03 打资源测试前快照）

### 1.1 质量门槛（刚实测）

| 项 | 结果 |
|---|---|
| 单元/集成测试 | ✅ **134 passed**（`multimodal-agent` 环境，10s） |
| 代码审计 | ✅ 0 发现（103 文件扫描，AUDIT PASSED） |
| 打资源 Simulator 闭环 | ✅ 可用（ResourceTask + Simulator） |
| 状态转换识别评测 | 🔄 有工具与基线，但 YOLO 独立能力未达标（见 1.4） |

### 1.2 模块进度（阶段 0~15）

| 阶段 | 内容 | 状态 |
|---|---|---|
| 0 | 环境与数据准备（43 类检测集、farm 合成集、爬虫数据） | ✅ |
| 1 | 视觉识别：YOLO 检测 + ResNet18 等级分类 + PaddleOCR + GameStateExtractor | ✅ |
| 2 | 状态管理与 FSM：GameState / FarmFSM / BattleFSM / DonationFSM | ✅ |
| 3/7 | 打资源闭环：ResourceTask（感知→决策→校验→执行→验证） | ✅（Simulator） |
| 4 | 资源目标优先度：0.40 价值 + 0.30 可达 + 0.20 安全 + 0.10 距离 | ✅ |
| 6 | 国产多模态大模型：qwen3-vl-plus 联调 + JSON Output + 级联路由 | ✅ |
| 8 | 行为执行器与模拟环境：AgentAction / Validator / Simulator | ✅ |
| 9 | FastAPI：/health /perceive /plan /cascade/stats /session/* | 🔄（缺 /execute /verify） |
| 13 | 实验评估体系：状态转换测试工具 + 基线 | 🔄 |
| 14 | 安全研究：强制审计 + 动作校验器 + 级联触发率统计 | 🔄 |
| 5/10/11/12/15 | 升级任务 / 捐兵 / Android / HarmonyOS / 真机交付 | ⬜ |

### 1.3 模型与数据资产

**检测模型（runs/detect/）**

| 模型 | 用途 | 关键指标 |
|---|---|---|
| `train/weights/best.pt` | 43 类基线 | 真实场景主用 |
| `farm/weights/best.pt` | farm 8 类（6 资源建筑） | 合成域 mAP50 0.90~0.99 |
| `fsm_v4/weights/best.pt` | 12 类 FSM（当前后端默认权重） | 线上默认 |
| `fsm_v5/weights/best.pt` | 17 类（12+捐兵 5） | mAP50 0.084 / mAP50-95 0.029，**数据不足，未达真机门槛** |

**状态分类器**：`runs/state_classifier_v5/best.pt`，val accuracy 0.8125。

**关键数据**（图片数量）

| 数据集 | 数量 | 说明 |
|---|---|---|
| `dataset/detection/` | 1500 | 43 类基线 |
| `dataset/farm_synthetic/` | 1460 | farm 合成集（6 资源建筑） |
| `dataset/video_batch_v5/` | 84 | 11 视频关键帧（68 训练 / 16 验证），488 框 |
| `dataset/llm_samples/` | 33 | cascade LLM 兜底回流样本 |
| `dataset/coach_sessions/` | 56 图 | coach 会话记录（含 v5 两轮 dry-run） |
| `dataset/llm_finetune/` | 2 | LLM 回流 → YOLO（9 框） |
| `dataset/fsm_real_buttons/` | 9 | OCR 半自动按钮标注（29 框） |
| `final_test_dataset/` | 9 | 真实截图 ground truth（评测用） |

### 1.4 打资源测试前必须知道的能力缺口

1. **v5 检测器数据量不足**：mAP50 0.084，按钮/兵种/建筑类别样本 <20/类，**不能直接切 ADB 真机**；
2. **YOLO 独立检出未达标**：9 张真实截图 YOLO 按钮/资源全 0，此前 100% 状态准确率来自 OCR 兜底；
3. **评测集与训练集重叠**：`fsm_real_buttons` 来自 `final_test_dataset`，当前数字不能代表泛化能力，**必须建独立评测集**；
4. **状态分布缺失**：战斗进行中 / 战斗结束 / 结算 / 捐兵详情-增援-完成 等截图不足；
5. **暂无 coach 抽样率旋钮**：Coach 目前每帧 100% 审查，缺少「渐进降低指导程度」的运行参数（计划在阶段 C 实现）。

### 1.5 已具备的「教师（强模型）→ 学生（弱模型）」基础设施

| 能力 | 位置 | 说明 |
|---|---|---|
| 级联路由（本地优先 + LLM 兜底 + 样本回流） | `perception/cascade.py` | mode=auto/local/llm；`/cascade/stats` 统计触发率与成本 |
| Coach 审查（LLM 逐帧批改本地输出） | `decision/coach.py` + `tools/coach_session.py` | 会话数据可回流重训 |
| 批量视频自动标注（OCR + LLM） | `tools/labeling/video_auto_label.py` | 强模型生产 YOLO 标签的主路径 |
| LLM 样本 → YOLO 标注 | `tools/labeling/llm_samples_to_yolo.py` | 回流转化 |
| OCR 半自动按钮标注 | `tools/labeling/ocr_buttons_to_yolo.py` | 文本兜底标注 |
| 合并微调 | `tools/labeling/finetune_fsm.py` | 12 类合并训练（需加类别权重） |
| 回流重训（会话 → 检测器 + 状态分类器） | `tools/coach_retrain.py` + `api/sessions.py` | `/session/retrain` 异步任务 |

**结论**：计划的工程底座已经存在，缺的是「**数据量**」与「**渐进退场的运行参数**」，本计划聚焦补这两块。

---

## 二、打资源测试：现在就能执行的入口

> 本轮测试分 5 个入口，按依赖从低到高排列；T1~T4 无需真机，T5 需要模拟器/真机。

| 入口 | 命令 | 验证点 | 现状 |
|---|---|---|---|
| **T1** Simulator 端到端闭环 | 见下 | FSM 逻辑、决策命中率、循环收敛、动作拦截 | ✅ 可跑 |
| **T2** 状态转换识别 | `python tools/eval_state_transition.py --images final_test_dataset --ocr` | 状态准确率 / 按钮召回 / YOLO 独立检出 | 🔄 有基线 |
| **T3** Coach 视频 dry-run | `python -m tools.coach_session --module farm --video <视频> --max-steps 50` | 本地感知 → 教练批改 → 动作执行记录 | ✅ v5 dry-run 已通 |
| **T4** API/控制台冒烟 | uvicorn + `/perceive` `/plan` `/cascade/stats` | 级联路由、触发率统计 | ✅ 已联调 |
| **T5** ADB 真机执行 | `python -m tools.coach_session --module farm --serial 127.0.0.1:5555` | 真实点击 + 前后帧状态验证 | ⬜ 待设备 |

### T1：Simulator 闭环（无模型、无网络，先跑通基线）

```bash
source activate multimodal-agent
python - <<'PY'
from executor.simulator import Simulator, default_farm_world
from tasks.resource import ResourceTask
result = ResourceTask().run(Simulator(default_farm_world()))
print(result.summary())
PY
```

记录：`status`（SUCCESS/STOP/ERROR/MAX_STEPS）、`total_loot`、步数。

### T2：状态转换识别（真实截图 + OCR 参考）

```bash
# 单帧 + OCR 参考（观察「YOLO 独立」与「OCR 兜底」的差距）
python tools/eval_state_transition.py --images final_test_dataset --ocr

# 序列模式：村庄待机→搜索中→战斗中→战斗结束→村庄待机
python tools/eval_state_transition.py --images final_test_dataset --ocr \
  --sequence village.jpg,search1.jpg,battle1.jpg,end1.jpg,village.jpg --json runs/eval_transition.json
```

门槛（本次+后续蒸馏后都要对照）：状态准确率 100%（信号规则判定）、按钮召回 ≥60%、敌方资源检出 ≥50% 帧。

### T3：Coach 视频 dry-run（真实视频驱动 + 强模型审查）

```bash
export VISION_MODEL_BASE_URL='https://dashscope.aliyuncs.com/compatible-mode/v1'
export VISION_MODEL_NAME='qwen3-vl-plus'
export VISION_MODEL_API_KEY="$(sed -n '2p' api.txt)"

python -m tools.coach_session --module farm \
  --video dataset/video/resource/<视频.mp4> --weights runs/detect/fsm_v4/weights/best.pt --max-steps 50
```

产物在 `dataset/coach_sessions/<时间戳>/`，重点看 `coach.json` 里教练对本地输出的「纠正次数 / 纠正原因」——这就是强模型指导程度的量化起点。

### T4：API / 控制台冒烟

```bash
source activate multimodal-agent
export VISION_MODEL_BASE_URL=... VISION_MODEL_NAME=qwen3-vl-plus VISION_MODEL_API_KEY="$(sed -n '2p' api.txt)"
uvicorn api.main:app --host 0.0.0.0 --port 8000
# 浏览器控制台：http://<电脑IP>:8000/static/index.html
# curl 冒烟：
curl -F "file=@final_test_dataset/<图>.jpg" "http://127.0.0.1:8000/perceive?mode=auto"
curl http://127.0.0.1:8000/cascade/stats
```

观察：`CASCADE_MODE`（默认 auto）、LLM 触发率、回流样本数 —— 作为阶段 C 退场的基线数据。

### T5：ADB 真机（条件具备后再做）

```bash
# 模拟器/真机开启 USB 调试或网络 ADB
tools/android-platform-tools/platform-tools/adb devices
python -m tools.coach_session --module farm --serial 127.0.0.1:5555 --max-steps 100
```

**本轮测试输出要求**：把 T1/T2/T3/T4 的结果（status、指标、障碍点）记入 PROGRESS.md 的「最近交付」。

---

## 三、后续计划：强模型指导 → 弱模型训练 → 渐进退场

### 总体循环

```
        ┌────────────────────── 阶段 A：强模型指导获取训练数据（Teacher）──────────────┐
        │  ① 批量视频标注 video_auto_label.py --llm（主路径，OCR + LLM 双来源）          │
        │  ② Coach 会话回流（tools/coach_session.py / /session/start）                  │
        │  ③ Cascade 兜底样本回流（dataset/llm_samples → llm_samples_to_yolo.py）       │
        │  ④ 人工/OCR 复核补标（tools/labeling/app.py + ocr_buttons_to_yolo.py）        │
        └───────────────┬──────────────────────────────────────────────────────────────┘
                        ▼
        ┌────────────────────── 阶段 B：弱模型蒸馏训练（Student）───────────────────────┐
        │  train_yolo.py（imgsz 640 + 类别加权 + 独立验证集）                            │
        │  + decision/state_classifier.py --train                                       │
        │  评估门槛未达标 → 主动学习回环：优先补 miss 帧 → 回阶段 A                      │
        └───────────────┬──────────────────────────────────────────────────────────────┘
                        ▼
        ┌────────────────────── 阶段 C：渐进退场（De-scaffolding）──────────────────────┐
        │  强模型始终在场；两个旋钮逐步下调：                                             │
        │   ① Cascade 触发阈值（min_avg_confidence 0.5→0.4→0.3、require_key_buildings）│
        │   ② Coach 抽样率（新增 coach_rate：100% → 50% → 只审低置信/分歧 → 只审报错） │
        │  每轮退场都以独立评测集 + /cascade/stats 触发率曲线为准，回退随时可逆          │
        └──────────────────────────────────────────────────────────────────────────────┘
```

### 阶段 A：强模型指导获取训练数据（Teacher）

目标：用 qwen3-vl-plus（+ OCR 交叉）把「打资源/捐兵真实录屏」变成高质量 YOLO 标签，弱模型只当 FSM 的信号识别器。

**A1 批量视频标注（主路径）**

```bash
python -m tools.labeling.video_auto_label \
  --video dataset/video/resource/*.mp4 dataset/video/donate/*.mp4 \
  --out dataset/video_batch_v6 --fps 0.25 --max-frames 60 --llm
```

- 采集重点（本轮最缺）：**战斗进行中 / 战斗结束 / 结算 / 捐兵详情 / 增援确认 / 增援完成**；
- 每类按钮/兵种/建筑 **≥20 框**，敌方 6 类资源建筑合计 **≥50 帧**；
- 状态覆盖要均衡：村庄待机（少）、搜索中、战斗中、战斗结束、结算、捐兵 4 环节。

**A2 Coach 会话回流（运行期教师-学生）**
- 跑 T3/T5 会话，教练纠正过的帧天然是「弱模型错 / 强模型对」的最有价值训练样本；
- 会话够量后用 `/session/retrain` 或 `tools/coach_retrain.py` 生成训练集。

**A3 Cascade 兜底样本回流**
- auto 模式下 LLM 兜底的难帧自动进 `dataset/llm_samples/`；
- 到量后 `python -m tools.labeling.llm_samples_to_yolo` 转成 YOLO 标注。

**A4 伪标签质量闸门（新增，防止教师噪声污染学生）**
- 只保留 LLM 输出结构化完整 + 坐标落在画面内 + 与 OCR/规则交叉一致或高置信(≥0.6) 的样本；
- 被过滤样本单独存 `rejected/` 并计数，方便诊断教师失败模式；
- 每批数据人工抽检 ≥10% 帧（`tools/labeling/app.py`）。

**产物**：`dataset/video_batch_v6/`（train/valid 按**视频**切分，不跨视频随机，杜绝帧泄漏）。

### 阶段 B：弱模型蒸馏训练（Student）

```bash
# 检测器：从 v5 热启动，imgsz 640，类别加权（按钮类 loss 加权，改动 finetune_fsm.py/train_yolo.py）
python -m tools.train_yolo \
  --dataset dataset/video_batch_v6 \
  --base runs/detect/fsm_v5/weights/best.pt \
  --out runs/detect/fsm_v6 --epochs 30 --imgsz 640

# 状态分类器
python -m decision.state_classifier \
  --train --data dataset/video_batch_v6/state_labels.json --out runs/state_classifier_v6
```

**评估门槛（exit gates，缺一不可）**

| 指标 | 门槛 |
|---|---|
| 按钮类检出率（YOLO 独立，独立评测集） | ≥60%（每类 ≥50%） |
| 敌方资源建筑检出率 | ≥50% 帧 |
| 状态分类器 val accuracy | ≥0.85 |
| 检测器 mAP50 | 显著高于 v5（0.084），至少翻倍 |
| 状态识别准确率（eval_state_transition） | 100%（信号规则判定） |

未达标 → **主动学习回环**：把评测 miss 的帧归入下轮 A 的标注队列，优先补这些帧（最省钱也最有效）。

### 阶段 C：强模型渐进退场（De-scaffolding）

> 原则（用户方案）：**强模型始终支持在场，弱模型学会后适当降低指导程度**。
> 退场不是删除强模型，而是降低它的调用概率；任何回退都可通过环境变量一键恢复。

**两个旋钮**

1. **Cascade 触发阈值**（`CascadeConfig` / `CASCADE_MODE` 环境变量）
   - `min_avg_confidence`：0.5 → 0.4 → 0.3（本地更有把握时才不需要兜底）
   - `require_key_buildings` / `CASCADE_MODE=auto|local`：auto 保留强模型兜底能力
2. **Coach 抽样率**（新增，建议加到 `SessionStartRequest` 与 `coach_session.py`）
   - `coach_rate=1.0` 每帧审 → `0.5` 抽审 → 仅低置信/状态分歧时审 → 仅报错时审
   - 低置信判定复用 cascade 的 `min_avg_confidence` 与 FSM 状态不一致检测

**退场节奏（每完成一轮 B 蒸馏后升一档，以门槛表为准）**

| 档位 | 时机 | CASCADE | Coach | LLM 触发率目标 |
|---|---|---|---|---|
| Stage 0 冷启动 | 数据不足期（现在） | `llm` 或 auto 高触发 | 100% 全审（攒数据） | 不设限，记录基线 |
| Stage 1 蒸馏后 | v6 训练完成、门槛达标 | auto，阈值 0.5→0.4 | 50% 抽审 | <30% |
| Stage 2 | 两轮以上蒸馏稳定 | auto，阈值 0.4→0.3 | 只审低置信/分歧 | <15% |
| Stage 3 稳态 | 独立评测集稳定 | auto（local 优先） | 只审报错/未知状态 | <10% |

**观测点**：`/cascade/stats`（本地/LLM 次数、触发率、触发原因、token、回流样本数）+ 每轮蒸馏后的独立评测集得分曲线。

**回退触发器**（任一命中即升一档指导，并回到 B 补数据）：
- 独立评测集按钮召回 < 门槛（回退到 Stage 0 阈值）；
- 状态分类器 accuracy 或 FSM 闭环成功率下滑 ≥5pt；
- 新增真实场景帧上出现系统性漏检（如新 UI 版本改版）。

### 阶段 D：打资源端到端验收

| 指标 | 门槛 |
|---|---|
| 值得/不值得 决策命中率（Simulator 闭环） | ≥80% |
| 动作校验拦截率（无效/越界漏网） | 0 |
| 循环收敛（N 村庄后资源全满 STOP，无死循环） | 必达 |
| 真机前后帧状态差异验证 | 每步动作后状态正确流转 |
| LLM 单帧兜底成本 | 稳态 <10% 帧触发 |

---

## 四、里程碑

| 里程碑 | 内容 | 验收 |
|---|---|---|
| **M0（本次）** | 跑通 T1~T4 打资源测试基线 | 指标记入 PROGRESS.md |
| **M1** | 数据扩充 v6：战斗/结算/捐兵缺环 + 每类 ≥20 框 + **独立验证集/评测集** | 数据清单与分布报告 |
| **M2** | v6 蒸馏训练（类别加权 + imgsz 640 + 热启动） | 门槛表全部达标 |
| **M3** | 打资源端到端 Simulator 闭环指标（决策命中率 ≥80%） | 指标报告 |
| **M4** | Coach/Cascade 渐进退场到 Stage 2~3 | LLM 触发率 <15%，回退机制演示 |
| **M5** | 真机 ADB + 控制台 + 捐兵 FSM（阶段 10） | 真机验收 |

## 五、用户待办

- [ ] 配置视觉大模型环境变量（`api.txt` 第 2 个 key → `VISION_MODEL_API_KEY`，见 `部署与安卓鸿蒙接入.md`）
- [ ] 补充录屏/截图：战斗进行中、战斗结束、结算、捐兵详情/增援确认/完成（每环节 ≥20 帧）
- [ ] 提供模拟器或真机（ADB）用于 T5 / M5
- [ ] 人工抽检/复核 LLM+OCR 生成的标注框（≥10% 帧）
- [ ] 确认一段打资源录屏放入 `dataset/video/resource/`（现在该目录为空，T3 依赖它）

## 六、命令速查

```bash
source activate multimodal-agent

# 测试与审计
python -m pytest tests -ra                 # 当前 134 passed
python -m security.audit                    # 当前 0 发现

# T1 Simulator 闭环
python - <<'PY'
from executor.simulator import Simulator, default_farm_world
from tasks.resource import ResourceTask
print(ResourceTask().run(Simulator(default_farm_world())).summary())
PY

# T2 状态转换识别
python tools/eval_state_transition.py --images final_test_dataset --ocr

# T3 Coach 视频 dry-run
python -m tools.coach_session --module farm --video dataset/video/resource/<视频>.mp4 \
  --weights runs/detect/fsm_v4/weights/best.pt --max-steps 50

# 阶段 A：批量视频标注
python -m tools.labeling.video_auto_label --video dataset/video/resource/* dataset/video/donate/* \
  --out dataset/video_batch_v6 --fps 0.25 --max-frames 60 --llm

# 阶段 B：蒸馏训练
python -m tools.train_yolo --dataset dataset/video_batch_v6 \
  --base runs/detect/fsm_v5/weights/best.pt --out runs/detect/fsm_v6 --epochs 30 --imgsz 640
python -m decision.state_classifier --train --data dataset/video_batch_v6/state_labels.json \
  --out runs/state_classifier_v6
```

---
